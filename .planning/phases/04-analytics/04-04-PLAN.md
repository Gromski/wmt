---
phase: 04-analytics
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/analytics.ts
  - lib/analytics.test.ts
  - app/analytics/page.tsx
  - components/analytics/TasteProfileRadar.tsx
autonomous: true
gap_closure: true
requirements: [ANALYTICS-01]

must_haves:
  truths:
    - "All four genre radars share ONE identical, ordered genre axis so their shapes are directly comparable across contributors (user UAT resolution 2026-08-13)"
    - "The shared axis = the top overlap genres (chosen by >=2 contributors, ranked by group-total count) PLUS the single top genre unique to each contributor (chosen by exactly one), capped at ~14 spokes, excluding the 'Unspecified' bucket"
    - "Each contributor's radar plots THEIR count for every axis genre (0 when they have none of it), aligned to the shared axis order"
    - "No PolarAngleAxis label is clipped at the /analytics card width (margins/outerRadius/tick sizing leave room for the outermost labels)"
    - "buildSharedGenreAxis is a pure, exported, unit-tested function; the radar still colours by CONTRIBUTOR_COLORS[initials]"
  artifacts:
    - path: "lib/analytics.ts"
      provides: "buildSharedGenreAxis(contributors) pure helper returning the ordered shared genre list"
      contains: "export function buildSharedGenreAxis"
    - path: "lib/analytics.test.ts"
      provides: "Unit coverage for overlap ranking, per-person unique inclusion, Unspecified exclusion, cap"
      contains: "buildSharedGenreAxis"
    - path: "app/analytics/page.tsx"
      provides: "Computes the shared axis once and passes axis-aligned {genre,count}[] to each TasteProfileRadar"
      contains: "buildSharedGenreAxis"
    - path: "components/analytics/TasteProfileRadar.tsx"
      provides: "Renders along a fixed axis with non-clipping margins/tick sizing"
      contains: "margin"
  key_links:
    - from: "app/analytics/page.tsx"
      to: "buildSharedGenreAxis"
      via: "compute once, map each contributor's genreBreakdown onto the shared axis (fill 0 for absent genres)"
      pattern: "buildSharedGenreAxis"
---

<objective>
Fix the genre-radar UAT quibble (labels cut off) with the design the user chose: make all four radars share ONE identical genre axis so their fingerprints are directly comparable, and cap the spoke count so labels fit. Today each radar independently renders that person's own 24–28 genres, which (a) clips the outer labels and (b) makes the four charts non-comparable.

Authoritative resolution (user, 2026-08-13): "The radars should have the same genres across each person so their shape is comparable. Include the ones we overlap on, plus one or two unique to each of us, depending on space."

Verified against live data, the shared axis is 14 spokes: top-10 overlap genres (Rock, Soul, Electronic, Funk, Jazz, Hip-Hop, New Wave, Indie, Pop, Folk) + one signature-unique per person (MW Synth-Pop, JG Post-Punk, JS Dubstep, IT Psychedelic).

Purpose: comparable, readable genre fingerprints with no clipped labels.
Output: a pure buildSharedGenreAxis helper + tests, page wiring, and a non-clipping radar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-analytics/04-CONTEXT.md
@.planning/phases/04-analytics/04-01-SUMMARY.md

<interfaces>
From lib/analytics.ts (existing — extend, don't rewrite):
```typescript
// getAnalyticsData(): Promise<AnalyticsData>
// AnalyticsData.contributors[]: { initials, name, topArtists, decadeHistogram,
//   genreBreakdown: { genre: string; count: number }[],  // per-person, sorted desc
//   genreProportions, decadeProportions, artistVector, genreVector, ... }
```

From components/analytics/TasteProfileRadar.tsx (existing):
```typescript
// props: { initials: string; data: { genre: string; count: number }[] }
// Renders Recharts RadarChart with <PolarAngleAxis dataKey="genre" />, coloured by CONTRIBUTOR_COLORS[initials].
// Currently: <ChartContainer className="mx-auto aspect-square max-h-[280px]"> with NO RadarChart margin → outer labels clip.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add buildSharedGenreAxis pure helper + tests</name>
  <files>lib/analytics.ts, lib/analytics.test.ts</files>
  <behavior>
    buildSharedGenreAxis(contributors: { initials: string; genreBreakdown: { genre: string; count: number }[] }[], opts?: { overlapCap?: number; uniquesPerPerson?: number }): string[]

    - Excludes the "Unspecified" genre entirely.
    - OVERLAP = genres present (count>0) for >=2 contributors, sorted by total group count desc; take the top `overlapCap` (default 10).
    - For each contributor, find their highest-count genre that is UNIQUE (present for exactly 1 contributor); append up to `uniquesPerPerson` (default 1) per contributor if not already in the axis.
    - Returns the ordered axis (overlap first in group-total order, then the per-person uniques in contributor order). Deterministic; stable tie-breaking by genre name.
    - Fixture test: 4 contributors where Rock/Soul are shared and each person has a distinct unique genre → axis contains the shared genres (ranked) + each person's unique; never contains "Unspecified"; length respects the caps.
  </behavior>
  <action>
    Implement and EXPORT buildSharedGenreAxis in lib/analytics.ts (pure — no DB/fetch). Build a genre→{presence,total,byInitials} tally from each contributor's genreBreakdown, skipping "Unspecified". Derive overlap (presence>=2) sorted by total desc, sliced to overlapCap. Then per contributor pick their top unique (presence===1) genre and append up to uniquesPerPerson, de-duplicated against the axis. Keep it small and readable, mirroring the existing helper style in lib/analytics.ts.

    Create lib/analytics.test.ts (new) using the existing node:assert/strict + "all assertions passed" convention (see lib/parse-playlist.test.ts / lib/similarity.test.ts). Cover: overlap ranking order, per-person unique inclusion, Unspecified exclusion, overlapCap/uniquesPerPerson caps, and a no-uniques contributor.
  </action>
  <verify>
    <automated>cd /Users/markwright/Development/warwick-massive-tunage && npx tsx lib/analytics.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <done>buildSharedGenreAxis exported + pure; analytics.test.ts prints "all assertions passed"; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared axis into the page + fix radar label clipping</name>
  <files>app/analytics/page.tsx, components/analytics/TasteProfileRadar.tsx</files>
  <action>
    In app/analytics/page.tsx: after getAnalyticsData(), compute `const genreAxis = buildSharedGenreAxis(data.contributors)` ONCE. For each contributor, build axis-aligned radar data: map genreAxis → { genre, count } using a lookup of that contributor's genreBreakdown (count 0 when absent). Pass this axis-aligned array to TasteProfileRadar (same genres, same order for all four → comparable shapes). Do not change the era/top-artist charts.

    In components/analytics/TasteProfileRadar.tsx: keep the { initials, data } props and CONTRIBUTOR_COLORS colouring, but FIX LABEL CLIPPING:
    - Add a `margin` to <RadarChart> (leave horizontal room for the outermost labels, e.g. top/bottom ~16, left/right ~64) and/or constrain PolarGrid/Radar via a smaller outerRadius so the label ring sits inside the ChartContainer box.
    - Set a compact <PolarAngleAxis ... tick={{ fontSize: 11 }} /> (and, if needed, a custom tick that truncates very long labels like "Singer-Songwriter"/"Drum & Bass" to a sensible width) so no label is clipped at the card width.
    - Widen the ChartContainer if needed (e.g. allow more than max-h-[280px] width, keep aspect-square) so 14 spokes fit.
    The data array now always has the same ~14 genres in the same order.
  </action>
  <verify>
    <automated>cd /Users/markwright/Development/warwick-massive-tunage && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>Page computes buildSharedGenreAxis once and passes axis-aligned data to all four radars; TasteProfileRadar has margin/tick sizing so no PolarAngleAxis label clips; typecheck + lint + build clean. (Final visual confirmation is a human browser check.)</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04-01 | Tampering | buildSharedGenreAxis over DB-derived genre counts | accept | Pure string/int math over already-imported, whitelisted genre data; no user input, no eval/fetch. |
| T-04-04-SC | Tampering | npm installs | n/a | No package installs — Recharts already present from 04-01. |
</threat_model>

<verification>
- `npx tsx lib/analytics.test.ts` prints "all assertions passed".
- `npm run typecheck`, `npm run lint`, `npm run build` pass; /analytics stays a static route.
- Static review: page calls buildSharedGenreAxis once and passes identical-ordered genre arrays to all four radars; TasteProfileRadar has a RadarChart margin + compact tick.
- Human browser check (deferred): all four radars show the same ~14 axes, shapes are visually comparable, and no label is cut off.
</verification>

<success_criteria>
- All four genre radars share one identical, ordered axis (top overlap genres + one signature-unique per person, excluding Unspecified), so shapes are directly comparable.
- No PolarAngleAxis label is clipped.
- buildSharedGenreAxis is pure, exported, and unit-tested; radars still colour by contributor.
- typecheck, lint, build, and the analytics test harness all pass.
</success_criteria>

<output>
Create `.planning/phases/04-analytics/04-04-SUMMARY.md` when done. Note that final visual confirmation (no clipping, comparable shapes) is a human browser check.
</output>
