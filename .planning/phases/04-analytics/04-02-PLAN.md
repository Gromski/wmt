---
phase: 04-analytics
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/similarity.ts
  - lib/similarity.test.ts
  - components/analytics/OverlapHeatmap.tsx
  - components/analytics/WildcardRanking.tsx
  - app/analytics/page.tsx
autonomous: true
requirements: [ANALYTICS-02, ANALYTICS-03]
must_haves:
  truths:
    - "The /analytics hub shows, at the top, a 4×4 overlap heatmap where each cell's colour intensity and in-cell number reflect how similar two friends' taste is"
    - "The hub shows a divergence ranking of all four friends with the top one highlighted as the 'wildcard'"
    - "Similarity scores are normalised to 0–1 and blend shared artists (weighted higher) with shared genres"
  artifacts:
    - path: "lib/similarity.ts"
      provides: "Pure cosine similarity + pairwise blend + group centroid + divergence ranking, with exported tuning constants"
      exports: ["cosineSimilarity", "pairwiseSimilarity", "buildOverlapMatrix", "divergenceRanking", "SIMILARITY_WEIGHTS", "PROFILE_WEIGHTS"]
    - path: "lib/similarity.test.ts"
      provides: "node:assert spec for the similarity/divergence math"
    - path: "components/analytics/OverlapHeatmap.tsx"
      provides: "Hand-rolled 4×4 CSS-grid heatmap coloured via CONTRIBUTOR_COLORS"
    - path: "components/analytics/WildcardRanking.tsx"
      provides: "Ranked divergence list highlighting the wildcard"
  key_links:
    - from: "app/analytics/page.tsx"
      to: "lib/similarity.ts"
      via: "buildOverlapMatrix + divergenceRanking over getAnalyticsData() vectors"
      pattern: "buildOverlapMatrix|divergenceRanking"
    - from: "components/analytics/OverlapHeatmap.tsx"
      to: "lib/contributor-colors.ts"
      via: "CONTRIBUTOR_COLORS[initials] cell colour"
      pattern: "CONTRIBUTOR_COLORS"
---

<objective>
Add the **group-overview** slice to the top of the `/analytics` hub (D-13): a 4×4 pairwise overlap heatmap (ANALYTICS-02) and a divergence ranking that surfaces the "wildcard" — the friend furthest from the group average (ANALYTICS-03).

Purpose: These two capabilities are the "how the group compares" half of the phase goal and share the same cosine-over-vectors math, so they ship together. They consume the per-contributor vectors already produced by `getAnalyticsData()` (Plan 01) — no new DB work.

Output: `lib/similarity.ts` (+ colocated test) holding the pure similarity/divergence math with tunable exported constants, `OverlapHeatmap` and `WildcardRanking` components, and a new group-overview section prepended above the taste profiles on the hub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics/04-CONTEXT.md
@.planning/phases/04-analytics/04-RESEARCH.md
@.planning/phases/04-analytics/04-PATTERNS.md

<interfaces>
<!-- Consumes the AnalyticsData contract created by Plan 01 (lib/analytics.ts). -->
<!-- Each ContributorAnalytics carries: artistVector, genreVector, profileVector (Record<string, number>). -->
<!-- These are plain records; convert to Map inside similarity.ts helpers if convenient, but similarity.ts's public API should accept Record<string, number> to stay serialisation-agnostic. -->

lib/similarity.ts must export at least:

```typescript
export const SIMILARITY_WEIGHTS: { artist: number; genre: number };   // ASSUMED 0.7 / 0.3 — tuning knob (RESEARCH A2)
export const PROFILE_WEIGHTS: { genre: number; era: number };         // ASSUMED 0.5 / 0.5 — tuning knob (RESEARCH A3)

export function cosineSimilarity(a: Record<string, number>, b: Record<string, number>): number;
export function pairwiseSimilarity(
  aArtist: Record<string, number>, aGenre: Record<string, number>,
  bArtist: Record<string, number>, bGenre: Record<string, number>,
): number;                                                            // 0–1, blended per SIMILARITY_WEIGHTS
export function buildOverlapMatrix(
  contribs: { artistVector: Record<string, number>; genreVector: Record<string, number> }[],
): number[][];                                                        // NxN, matrix[i][j] symmetric, diagonal = 1
export function divergenceRanking(
  contribs: { initials: string; name: string; profileVector: Record<string, number> }[],
): { initials: string; name: string; divergence: number }[];         // desc by divergence; [0] = wildcard
```
</interfaces>

@lib/contributor-colors.ts
@components/SessionCard.tsx
@lib/parse-playlist.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/similarity.ts pure math + colocated test</name>
  <files>lib/similarity.ts, lib/similarity.test.ts</files>
  <read_first>
    - lib/parse-playlist.test.ts (the established colocated pure-function test convention: `node:assert/strict`, plain assertions, runnable via `npx tsx <file>` — copy the header note about no configured runner)
    - .planning/phases/04-analytics/04-RESEARCH.md § Pattern 4 (cosineSimilarity + SIMILARITY_WEIGHTS + pairwiseSimilarity, lines ~307-339) and § Pattern 5 (groupCentroid + cosine distance + PROFILE_WEIGHTS, lines ~341-376)
    - .planning/phases/04-analytics/04-PATTERNS.md § "lib/similarity.ts" (individually-exported named pure functions, no class/default export)
  </read_first>
  <behavior>
    - cosineSimilarity of a vector with itself = 1; with a disjoint vector = 0; with an empty vector = 0 (no divide-by-zero)
    - cosineSimilarity is symmetric: cos(a,b) === cos(b,a)
    - pairwiseSimilarity blends artist and genre cosines by SIMILARITY_WEIGHTS (0.7/0.3) and stays within [0,1]
    - buildOverlapMatrix returns a symmetric NxN matrix with diagonal entries = 1 (each contributor vs self)
    - divergenceRanking returns all contributors sorted by divergence descending; divergence = 1 − cosineSimilarity(profileVector, group centroid); centroid is the per-key average of all profile vectors; the highest-divergence contributor is first (the wildcard)
  </behavior>
  <action>
    Implement `lib/similarity.ts` per RESEARCH Patterns 4 & 5, exporting the `<interfaces>` API as individually-named pure functions (no class, no default export — match `lib/parse-playlist.ts`). Accept `Record<string, number>` on the public API (convert to `Map` internally if helpful). Export `SIMILARITY_WEIGHTS = { artist: 0.7, genre: 0.3 }` and `PROFILE_WEIGHTS = { genre: 0.5, era: 0.5 }` as named `const` objects with `[ASSUMED — post-UAT tuning knob]` comments (RESEARCH A2/A3) so the group can retune in one line. `buildOverlapMatrix` computes `pairwiseSimilarity` for every ordered pair (diagonal forced to 1). `divergenceRanking` builds the group centroid (per-key mean of all `profileVector`s) then ranks each contributor by `1 - cosineSimilarity(profileVector, centroid)`, sorted descending. Guard all norms against zero.

    Write `lib/similarity.test.ts` colocated, using `node:assert/strict` plain assertions covering the `<behavior>` cases (self=1, disjoint=0, empty=0, symmetry, blend weighting, matrix diagonal/symmetry, ranking order + wildcard = index 0). Copy the header note from `parse-playlist.test.ts` explaining it runs via `npx tsx lib/similarity.test.ts` (no vitest/jest configured). Keep the test free of DB/import-chain dependencies so it runs standalone.
  </action>
  <verify>
    <automated>npx tsx lib/similarity.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx lib/similarity.test.ts` exits 0 and prints an all-assertions-passed line
    - `lib/similarity.ts` exports `cosineSimilarity`, `pairwiseSimilarity`, `buildOverlapMatrix`, `divergenceRanking`, `SIMILARITY_WEIGHTS`, `PROFILE_WEIGHTS`
    - `cosineSimilarity(v, v) === 1` and `cosineSimilarity(v, {}) === 0` (no NaN)
    - `buildOverlapMatrix` output is symmetric with diagonal 1; `divergenceRanking` is sorted divergence-descending
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>Similarity and divergence math is implemented as tested pure functions with exported tuning constants.</done>
</task>

<task type="auto">
  <name>Task 2: Overlap heatmap + wildcard ranking + group-overview section</name>
  <files>components/analytics/OverlapHeatmap.tsx, components/analytics/WildcardRanking.tsx, app/analytics/page.tsx</files>
  <read_first>
    - components/SessionCard.tsx (plain server component with a typed payload prop, `Card`/`CardContent` wrapper — no `"use client"` for presentational grids/cards)
    - lib/contributor-colors.ts + components/ContributorChip.tsx (CONTRIBUTOR_COLORS cell/badge colouring)
    - app/analytics/page.tsx (the hub created by Plan 01 — prepend the new section above the taste profiles per D-13; do NOT restructure the taste section)
    - .planning/phases/04-analytics/04-RESEARCH.md § Pattern 7 (hand-rolled CSS-grid heatmap, lines ~404-451, incl. cellStyle opacity `0.15 + score*0.85`) and § D-10 (colour intensity + in-cell score)
    - lib/similarity.ts (buildOverlapMatrix, divergenceRanking — from Task 1)
  </read_first>
  <action>
    Create `components/analytics/OverlapHeatmap.tsx` as a plain Server Component (NO `"use client"`) rendering the 4×4 matrix as a CSS grid per RESEARCH Pattern 7: row/column headers = contributor initials, each cell coloured `CONTRIBUTOR_COLORS[rowInitials].bg` at opacity `0.15 + score*0.85`, showing `score.toFixed(2)` (diagonal shows "—"). Export a typed prop interface (`contributors: {initials,name}[]`, `matrix: number[][]`) like SessionCard does. Use `Card`/`CardContent` for the container. Give it a fixed React `key` per cell (avoid `<>` fragment-in-map without keys — use explicit keys to satisfy Biome/lint).

    Create `components/analytics/WildcardRanking.tsx` as a plain Server Component rendering the `divergenceRanking` output as an ordered list: each row shows contributor name (in their colour) + divergence score; the top entry ([0]) is visually highlighted and labelled the "wildcard" with fun/personal (not clinical) copy per the CONTEXT specifics note. All four are shown (D-07: ranked, not just one name).

    Modify `app/analytics/page.tsx`: after `getAnalyticsData()`, compute `matrix = buildOverlapMatrix(data.contributors)` and `ranking = divergenceRanking(data.contributors)`, then render a new "Group overview" section (heading + `OverlapHeatmap` + `WildcardRanking`) ABOVE the taste-profiles section (D-13 order: group overview → taste profiles). Pass a `contributors: {initials,name}[]` list (derived from `data.contributors`) to the heatmap so its axis order matches the matrix order.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `/analytics` renders a group-overview section above the taste profiles containing a 4×4 heatmap and a four-row wildcard ranking with the top entry highlighted
    - `OverlapHeatmap.tsx` and `WildcardRanking.tsx` are plain server components (no `"use client"`) and colour via `CONTRIBUTOR_COLORS`
    - Heatmap cells show a 2-decimal score (diagonal "—") with opacity scaling by score
    - `npm run lint`, `npm run typecheck`, `npm run build` all pass
  </acceptance_criteria>
  <done>The hub's top section shows a working overlap heatmap and wildcard ranking derived from the similarity math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `/analytics` RSC | Public, read-only, no user-supplied parameters — unchanged from Plan 01. This plan adds only server-computed sections and pure math; no new input surface. |
| `getAnalyticsData()` output → `lib/similarity.ts` | Internal, trusted data (already-aggregated numeric vectors); no external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `lib/similarity.ts` numeric math | accept | Pure functions over trusted in-process vectors; no I/O, no user input, no injection surface. Correctness guarded by `lib/similarity.test.ts`. |
| T-04-05 | Information Disclosure | Heatmap/wildcard rendering | accept | Only aggregate similarity scores over already-public contributor data are shown; no secrets or PII introduced. |
| T-04-06 | Denial of Service | O(N²) matrix + centroid over 4 contributors | accept | N = 4 — constant, trivial cost; route stays statically renderable. |
</threat_model>

<verification>
- `npx tsx lib/similarity.test.ts` passes
- Overlap heatmap diagonal reads "—"; off-diagonal cells are symmetric (matrix[i][j] === matrix[j][i]) and in [0,1]
- Wildcard ranking lists all four, top one highlighted; order matches `divergenceRanking`
- `npm run build` keeps `/analytics` statically rendered
</verification>

<success_criteria>
- A user sees, at the top of `/analytics`, a 4×4 overlap heatmap (ANALYTICS-02) and a divergence ranking highlighting the wildcard (ANALYTICS-03).
- Tuning constants `SIMILARITY_WEIGHTS` / `PROFILE_WEIGHTS` are single-line exports for post-UAT adjustment.
</success_criteria>

<output>
Create `.planning/phases/04-analytics/04-02-SUMMARY.md` when done.
</output>
