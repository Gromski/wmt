---
phase: 04-analytics
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/wrapped.ts
  - lib/wrapped.test.ts
  - components/analytics/WrappedCard.tsx
  - app/analytics/page.tsx
autonomous: true
requirements: [ANALYTICS-04]
must_haves:
  truths:
    - "The /analytics hub shows, inline below the taste profiles, one bold Spotify-Wrapped-style card per friend in their contributor colour"
    - "Each card shows signature genre + #1 artist, a group-unique pick (an artist/genre only they chose), era range (oldest & newest track year), and headline counts (tracks, distinct artists, sessions appeared in)"
    - "Sessions-appeared-in counts reflect each person's real attributed sessions (not hardcoded 31) so 3-person sessions 25/28 show correctly"
  artifacts:
    - path: "lib/wrapped.ts"
      provides: "computeWrappedStats(): per-contributor signature genre, #1 artist, group-unique pick, era range, headline counts"
      exports: ["computeWrappedStats"]
    - path: "lib/wrapped.test.ts"
      provides: "node:assert spec for exclusivity + era-range + headline-count logic"
    - path: "components/analytics/WrappedCard.tsx"
      provides: "Bold per-contributor Wrapped card (server component)"
  key_links:
    - from: "app/analytics/page.tsx"
      to: "lib/wrapped.ts"
      via: "computeWrappedStats(data)"
      pattern: "computeWrappedStats"
    - from: "components/analytics/WrappedCard.tsx"
      to: "lib/contributor-colors.ts"
      via: "CONTRIBUTOR_COLORS[initials] card colour"
      pattern: "CONTRIBUTOR_COLORS"
---

<objective>
Add the final slice: an inline **Wrapped-cards** section on the `/analytics` hub (D-11/D-15) — one vivid Spotify-Wrapped-style card per friend, in their contributor colour, showing headline stats and standout picks (ANALYTICS-04).

Purpose: Completes the phase. The meaty new computation is the **group-unique pick** (D-12b) — an artist (or, failing that, genre) that ONLY that person chose across all 31 sessions — plus era range and edge-case-correct headline counts. Consumes the `attributedRows` + per-contributor aggregates already returned by `getAnalyticsData()` (Plan 01).

Output: `lib/wrapped.ts` (+ colocated test) computing the four standout stats per D-12, a `WrappedCard` component, and a Wrapped section appended below the taste profiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics/04-CONTEXT.md
@.planning/phases/04-analytics/04-RESEARCH.md
@.planning/phases/04-analytics/04-PATTERNS.md

<interfaces>
<!-- Consumes the AnalyticsData contract from Plan 01 (lib/analytics.ts):
     data.contributors[].topArtists / genreBreakdown, and data.attributedRows (flat rows
     with contributorInitials, artistName, primaryGenre, releaseYear, sessionId). -->

lib/wrapped.ts must export:

```typescript
export type WrappedStats = {
  initials: string;
  name: string;
  signatureGenre: string;            // top genreBreakdown entry (excl "Unspecified" if a real genre exists)
  topArtist: string;                 // #1 topArtists entry
  groupUniquePick: { kind: "artist" | "genre"; value: string } | null; // artist-first, genre fallback (D-12b)
  eraRange: { oldest: number | null; newest: number | null };          // by release_year (D-12c)
  headlineCounts: { tracks: number; distinctArtists: number; sessions: number }; // D-12d
};

// Accepts the Plan 01 AnalyticsData (or the minimal subset it needs).
export function computeWrappedStats(data: import("./analytics").AnalyticsData): WrappedStats[];
```
</interfaces>

@lib/contributor-colors.ts
@components/SessionCard.tsx
@components/ContributorChip.tsx
@lib/parse-playlist.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/wrapped.ts stat computation + colocated test</name>
  <files>lib/wrapped.ts, lib/wrapped.test.ts</files>
  <read_first>
    - lib/analytics.ts (the AnalyticsData shape from Plan 01 — `contributors[].topArtists`/`genreBreakdown`, `attributedRows`)
    - lib/parse-playlist.test.ts (colocated `node:assert/strict` test convention, runnable via `npx tsx`)
    - .planning/phases/04-analytics/04-RESEARCH.md § Pattern 6 (findExclusiveArtists — artist chosen by exactly one contributor, lines ~378-402), § Pitfall 3 (sessions-appeared-in = COUNT(DISTINCT sessionId), NOT hardcoded 31; sessions 25/28 are 3-person), § Pitfall 4 (Unknown/Unspecified buckets), and § Open Question 1 (prefer artist-exclusivity, fall back to genre-exclusivity)
    - .planning/phases/04-analytics/04-CONTEXT.md § D-12 (the four standout stats a/b/c/d)
  </read_first>
  <behavior>
    - signatureGenre = the highest-count genre in genreBreakdown, preferring a real genre over "Unspecified" when the person has any real-genre tracks
    - topArtist = the #1 entry of topArtists
    - groupUniquePick: an artist chosen by exactly ONE contributor across all attributedRows → that contributor's exclusive artist (ranked by their track count for that artist); if a contributor has no exclusive artist, fall back to a genre only they chose; if neither exists, null
    - eraRange.oldest / .newest = min / max non-null release_year among the contributor's attributedRows (both null if they have no dated tracks)
    - headlineCounts.tracks = count of the contributor's attributedRows; distinctArtists = distinct artistName; sessions = COUNT(DISTINCT sessionId) — so an absent-from-some-sessions person can read < 31 (Pitfall 3)
  </behavior>
  <action>
    Implement `lib/wrapped.ts` exporting `computeWrappedStats(data)` returning one `WrappedStats` per contributor (order MW, JG, JS, IT). Derive `signatureGenre`/`topArtist` from the per-contributor aggregates. Compute exclusivity globally over `data.attributedRows` per RESEARCH Pattern 6: build an `artistName → Set<contributorInitials>` map; an artist is exclusive to a contributor iff the set size is 1. For each contributor pick their highest-track-count exclusive artist as `groupUniquePick={kind:"artist"}`; if none, repeat the same exactly-one-chooser logic over `primaryGenre` (excluding null/"Unspecified") for a `{kind:"genre"}` pick; else null (RESEARCH Open Question 1 resolution). Compute `eraRange` from min/max non-null `releaseYear`, and `headlineCounts` with `sessions` as distinct `sessionId` count (never hardcode 31 — Pitfall 3). Pure functions, individually exported, no I/O — match `lib/similarity.ts`/`lib/parse-playlist.ts` style.

    Write `lib/wrapped.test.ts` colocated (`node:assert/strict`, `npx tsx`-runnable, copy the header note): construct a small synthetic `AnalyticsData`-shaped fixture and assert exclusive-artist detection (an artist chosen by two people is NOT unique to either; one chosen by exactly one IS), the genre fallback when no exclusive artist, era range min/max with a null year present, and `sessions` distinct-count behaviour (a contributor present in only 2 of 3 fixture sessions reads 2).
  </action>
  <verify>
    <automated>npx tsx lib/wrapped.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx lib/wrapped.test.ts` exits 0 with an all-assertions-passed line
    - `lib/wrapped.ts` exports `computeWrappedStats` and the `WrappedStats` type per the `<interfaces>` contract
    - An artist chosen by more than one contributor is never returned as anyone's group-unique pick
    - `headlineCounts.sessions` is a distinct-sessionId count (can be < 31), not a constant
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>Per-contributor Wrapped stats (signature genre, #1 artist, group-unique pick, era range, headline counts) are computed and tested.</done>
</task>

<task type="auto">
  <name>Task 2: WrappedCard component + inline Wrapped section</name>
  <files>components/analytics/WrappedCard.tsx, app/analytics/page.tsx</files>
  <read_first>
    - components/SessionCard.tsx (Card/CardContent structure, plain server component with typed payload prop)
    - components/ContributorChip.tsx (CONTRIBUTOR_COLORS[initials] colour resolution, lines ~22-30)
    - components/ui/card.tsx (Card/CardContent/CardHeader/CardTitle primitives)
    - app/analytics/page.tsx (the hub — append the Wrapped section BELOW the taste profiles per D-13/D-15; do not restructure existing sections)
    - .planning/phases/04-analytics/04-CONTEXT.md § D-11 (bold Spotify-Wrapped aesthetic, one vivid card per person in their colour) + § specifics (fun/personal, not clinical copy)
    - lib/wrapped.ts (computeWrappedStats / WrappedStats — from Task 1)
  </read_first>
  <action>
    Create `components/analytics/WrappedCard.tsx` as a plain Server Component (NO `"use client"` — presentational, no client state, per RESEARCH Architectural Responsibility Map) taking a `stats: WrappedStats` prop. Render a bold, vivid card: background/accent from `CONTRIBUTOR_COLORS[stats.initials]` (bg + fg for readable text), the contributor name as a big headline, then the four standout blocks — signature genre + #1 artist; the group-unique pick with fun framing (e.g. "Only <name> played <value>") and a graceful fallback when null; era range ("<oldest>–<newest>", or a friendly note if both null); and headline counts (tracks / distinct artists / sessions appeared in). Use `Card`/`CardContent` rather than raw divs. Keep copy fun/personal per the CONTEXT specifics note. Never introduce a second colour source (only CONTRIBUTOR_COLORS).

    Modify `app/analytics/page.tsx`: compute `wrapped = computeWrappedStats(data)` and append a "Wrapped" section (heading + a responsive grid of four `WrappedCard`s) BELOW the taste-profiles section (D-13 final order: group overview → taste profiles → Wrapped cards). This is the last section — no dedicated per-person routes (D-15).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `/analytics` renders four Wrapped cards inline below the taste profiles, each in its contributor colour, showing signature genre + #1 artist, group-unique pick, era range, and headline counts
    - `WrappedCard.tsx` is a plain server component colouring via `CONTRIBUTOR_COLORS` and gracefully handles a null group-unique pick and null era range
    - Section order on the page is: group overview → taste profiles → Wrapped cards (D-13)
    - `npm run lint`, `npm run typecheck`, `npm run build` all pass
  </acceptance_criteria>
  <done>The hub shows four bold Wrapped cards with real standout stats — ANALYTICS-04 satisfied and the phase feature-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `/analytics` RSC | Public, read-only, no user-supplied parameters — unchanged. This plan adds a server-computed section and pure math only; no new input surface. |
| `getAnalyticsData()` output → `lib/wrapped.ts` | Internal trusted data (already-aggregated rows); no external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | `lib/wrapped.ts` exclusivity/count math | accept | Pure functions over trusted in-process rows; no I/O or user input. Correctness guarded by `lib/wrapped.test.ts`. |
| T-04-08 | Information Disclosure | Wrapped card content | accept | Cards show only aggregate stats over already-public contributor/track data (Phase 3); no secrets or PII introduced. The 3-person-session count (Pitfall 3) is correct data, surfaced honestly. |
| T-04-09 | Denial of Service | Exclusivity pass over attributedRows | accept | ~474 rows, single linear pass; route stays statically rendered. |
</threat_model>

<verification>
- `npx tsx lib/wrapped.test.ts` passes
- Every Wrapped card's "sessions appeared in" equals a distinct-sessionId count (whoever missed sessions 25/28 reads < 31)
- Group-unique pick, when present, names an artist/genre chosen by exactly one contributor
- `npm run build` keeps `/analytics` statically rendered; final section order matches D-13
</verification>

<success_criteria>
- A user sees four bold, colour-coded Wrapped cards inline on `/analytics` with headline stats and standout picks — ANALYTICS-04 satisfied.
- Phase 4 is feature-complete: taste profiles (Plan 01), group overview (Plan 02), Wrapped cards (Plan 03) on a single hub.
</success_criteria>

<output>
Create `.planning/phases/04-analytics/04-03-SUMMARY.md` when done.
</output>
