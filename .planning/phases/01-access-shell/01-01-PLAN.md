---
phase: 01-access-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml-or-package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - .gitignore
  - .env.local
  - .env.example
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/db/index.ts
  - src/lib/db/schema.ts
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/app/api/auth/[...all]/route.ts
  - drizzle.config.ts
  - drizzle/
autonomous: false
requirements:
  - ACCESS-01
  - ACCESS-04
user_setup:
  - service: spotify-developer
    why: "Spotify OAuth requires a registered app to obtain Client ID and Client Secret. The four friends cannot log in without these credentials."
    env_vars:
      - name: SPOTIFY_CLIENT_ID
        source: "developer.spotify.com/dashboard → Create app → Client ID"
      - name: SPOTIFY_CLIENT_SECRET
        source: "developer.spotify.com/dashboard → Create app → View client secret"
    dashboard_config:
      - task: "Create Spotify app in Dev Mode"
        location: "developer.spotify.com/dashboard → Create app"
        details: "Name: Warwick Massive Tunage. Redirect URI MUST be http://127.0.0.1:3000/api/auth/callback/spotify (NOT localhost — Spotify blocked localhost on 27 Nov 2025). APIs used: Web API. Premium account required by the app owner per Feb 2026 changes."

must_haves:
  truths:
    - "Running `npm run dev` boots the app on http://127.0.0.1:3000 with no errors"
    - "GET / returns 200 and renders the empty-state archive shell ('No sessions yet') without any auth check"
    - "GET /api/auth/session returns a JSON body (Better Auth handler responds, even if session is null)"
    - "The local SQLite file (local.db) exists with Better Auth tables (user, session, account) AND the role column on user"
    - "process.env.BETTER_AUTH_SECRET and SPOTIFY_CLIENT_ID/SECRET are present in .env.local (developer has run Spotify dashboard setup)"
  artifacts:
    - path: src/lib/auth.ts
      provides: "Better Auth server instance with Spotify provider, Drizzle adapter, role additionalField, first-user-is-admin hook"
      contains: "betterAuth("
    - path: src/lib/auth-client.ts
      provides: "Better Auth React client for Client Components"
      contains: "createAuthClient"
    - path: src/lib/db/schema.ts
      provides: "Drizzle schema with Better Auth tables (user, session, account, verification) plus role column on user"
      contains: "role"
    - path: src/lib/db/index.ts
      provides: "Drizzle db instance backed by libSQL"
      contains: "drizzle"
    - path: src/app/page.tsx
      provides: "Public archive empty-state page (no auth check, ready for Phase 2 data)"
      contains: "No sessions yet"
    - path: src/app/layout.tsx
      provides: "Root layout with Inter font, ThemeProvider (dark default), Toaster"
      contains: "ThemeProvider"
    - path: src/app/api/auth/[...all]/route.ts
      provides: "Better Auth catch-all route handler for /api/auth/*"
      contains: "toNextJsHandler"
    - path: drizzle.config.ts
      provides: "Drizzle Kit config pointing at libSQL with schema path src/lib/db/schema.ts"
      contains: 'dialect: "turso"'
    - path: next.config.ts
      provides: "Next.js config with cacheComponents flag enabled (per RESEARCH Pitfall 4)"
      contains: "cacheComponents"
    - path: .env.local
      provides: "Local dev environment with BETTER_AUTH_SECRET, BETTER_AUTH_URL, SPOTIFY_CLIENT_ID/SECRET, TURSO_CONNECTION_URL=file:local.db"
      contains: "BETTER_AUTH_URL=http://127.0.0.1:3000"
  key_links:
    - from: src/lib/auth.ts
      to: src/lib/db/index.ts
      via: "drizzleAdapter(db, { provider: 'sqlite' })"
      pattern: "drizzleAdapter\\(db"
    - from: src/app/api/auth/[...all]/route.ts
      to: src/lib/auth.ts
      via: "toNextJsHandler(auth)"
      pattern: "toNextJsHandler\\(auth\\)"
    - from: drizzle.config.ts
      to: src/lib/db/schema.ts
      via: 'schema: "./src/lib/db/schema.ts"'
      pattern: "src/lib/db/schema.ts"
---

## Phase Goal

**As a** Warwick session friend (MW, JG, JS, or IT), **I want to** sign in with Spotify and reach a private dashboard while the public archive page stays open to anyone, **so that** the four of us can use authenticated tools without locking visitors out of the read-only archive.

> Plan 01 of 3 — Walking Skeleton foundation. Establishes the scaffolded Next.js 16 project, Drizzle/libSQL database, Better Auth Spotify provider configuration, public archive empty state, and the Better Auth route handler. Together with Plans 02 and 03, completes the vertical slice that satisfies ACCESS-01 through ACCESS-04.

<objective>
Stand up the Walking Skeleton's foundation: scaffold the Next.js 16 + Tailwind v4 + TypeScript project; install the verified package set from RESEARCH.md §Standard Stack; wire Drizzle ORM to a local libSQL file with a schema that includes Better Auth's generated tables plus the `role: 'admin' | 'member'` additionalField column (D-05); configure Better Auth with the Spotify social provider requesting the four scopes locked in D-08 and the first-user-is-admin database hook (D-06); mount the Better Auth catch-all route handler; build the public-archive empty state (D-02, D-04, ACCESS-04); and push the schema to local SQLite.

Purpose: Produce the smallest possible end-to-end-working skeleton — `npm run dev` boots, `GET /` renders without auth, `GET /api/auth/session` responds, and the local DB has the right shape ready for the OAuth flow that Plan 02 wires up.

Output: Working Next.js project with the scaffold, dependencies, DB schema, auth configuration, and public empty-state page in place. After this plan, the dev server runs; after Plan 02, OAuth works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-access-shell/01-CONTEXT.md
@.planning/phases/01-access-shell/01-RESEARCH.md
@.planning/phases/01-access-shell/01-PATTERNS.md
@.planning/phases/01-access-shell/01-UI-SPEC.md
@.planning/phases/01-access-shell/SKELETON.md
</context>

<interfaces>
Contracts produced by this plan that Plans 02 and 03 consume:

`src/lib/auth.ts` — exports `auth` with these consumer-facing affordances:
- `auth.api.getSession({ headers: await headers() })` returns either `null` or `{ user: { id, email, name, image, role }, session: { ... } }`. `user.role` is the custom `additionalField` defined here with values `'admin'` or `'member'` (default `'member'`).
- `auth.handler` is wrapped by `toNextJsHandler` and exported through the route handler.

`src/lib/auth-client.ts` — exports `authClient` with:
- `authClient.signIn.social({ provider: "spotify", callbackURL: "/dashboard" })`
- `authClient.signOut({ fetchOptions: { onSuccess: () => router.push("/") } })`
- `authClient.useSession()` returning `{ data, isPending, error }`

`src/lib/db/schema.ts` — exports tables `user`, `session`, `account`, `verification`. `user` has the additional column `role: text("role").notNull().default("member")`.

`src/lib/db/index.ts` — exports `db` (Drizzle libSQL instance) for use anywhere on the server.

Environment contract (`.env.local`, never committed):
- `BETTER_AUTH_SECRET` — 32-byte base64, used by Better Auth for cookie signing
- `BETTER_AUTH_URL=http://127.0.0.1:3000` — MUST be `127.0.0.1` not `localhost`
- `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET` — from Spotify Developer Dashboard
- `TURSO_CONNECTION_URL=file:local.db` (local dev); `TURSO_AUTH_TOKEN` omitted in dev
- `NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000` — read by `auth-client.ts`
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Spotify Developer App — manual setup (human-only)</name>
  <read_first>
    - .planning/phases/01-access-shell/01-CONTEXT.md (D-08 scope list)
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 444-449 Pitfall 1 on 127.0.0.1; lines 632-642 Environment Availability)
    - CLAUDE.md §Spotify Integration Details (Feb 2026 changes, Dev Mode 5-user cap, Premium requirement)
  </read_first>
  <files>none — human-only setup; values captured into .env.local by Task 3</files>
  <action>Pause execution. Present the `<how-to-verify>` steps to the user and wait for them to register the Spotify Developer app and reply with SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET. Capture both secret strings in scratch context so Task 3 can write them into .env.local — never echo them back to the user, never log them, never commit them. If the user replies `blocked: <reason>`, halt the plan and surface the blocker to the orchestrator.</action>
  <verify>
    <human-check>User has supplied SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET, confirmed the redirect URI `http://127.0.0.1:3000/api/auth/callback/spotify` is registered, and confirmed the Spotify account is Premium.</human-check>
  </verify>
  <what-built>Nothing yet — this is a human-only prerequisite. Spotify does not expose a CLI/API to create a Developer Dashboard app, so the project owner must register the app manually before any OAuth code can be tested.</what-built>
  <how-to-verify>
    1. Open https://developer.spotify.com/dashboard and sign in with the Spotify account that will own the app (must be Premium per Feb 2026 changes — CLAUDE.md §Spotify Integration Details).
    2. Click "Create app". Fill in:
       - App name: Warwick Massive Tunage
       - App description: Private archive and analytics for our themed music sessions.
       - Redirect URI: http://127.0.0.1:3000/api/auth/callback/spotify (this exact value — NOT localhost; Spotify blocked localhost on 27 Nov 2025)
       - APIs used: tick "Web API"
       - Tick the Developer Terms checkbox.
    3. After creation, open the app and click "Settings". Copy the Client ID. Click "View client secret" and copy the Client Secret.
    4. Paste both into `.env.local` in Task 2 (the executor will create the file). The Client ID and Client Secret values are required before Task 6 (the schema push) so the rest of the plan can run.
    5. Confirm the Spotify account is on Premium (Settings → Account → Subscription) — Feb 2026 changes require the app owner to maintain Premium.
    6. Confirm Dev Mode user cap is 5 — fine for the 4-friend team.
  </how-to-verify>
  <acceptance_criteria>
    - SPOTIFY_CLIENT_ID is a non-empty string (32 hex chars typical) supplied by the user
    - SPOTIFY_CLIENT_SECRET is a non-empty string supplied by the user
    - Redirect URI registered in Spotify dashboard is exactly `http://127.0.0.1:3000/api/auth/callback/spotify`
    - User confirms the Spotify account is Premium
  </acceptance_criteria>
  <resume-signal>Reply with `approved` plus the two values (the executor will write them to .env.local in Task 2). Or reply `blocked: <reason>` if the dashboard work cannot be completed now.</resume-signal>
  <done>User has provided SPOTIFY_CLIENT_ID, SPOTIFY_CLIENT_SECRET, and confirmed the 127.0.0.1 redirect URI is registered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify npm package legitimacy (slopcheck fallback)</name>
  <read_first>
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 157-177 Package Legitimacy Audit; lines 599-606 Assumptions Log A4)
  </read_first>
  <files>none — human-verify gate; gates Task 3's install step</files>
  <action>Pause execution. Present the eight npm registry URLs from `<how-to-verify>` to the user and wait for them to confirm each package matches its source repo. If the user replies `approved`, proceed to Task 3. If the user replies `blocked: <package>`, halt and surface the package name to the orchestrator — do NOT install anything until the human approves.</action>
  <verify>
    <human-check>User has replied `approved` after manually confirming each of the eight packages on the npm registry pages.</human-check>
  </verify>
  <what-built>Nothing yet — this is a security gate before the install. RESEARCH.md flagged all packages as [ASSUMED] because slopcheck was unavailable at research time. STRIDE threat T-01-SC requires a blocking human checkpoint per `<security_context>` ASVS L1.</what-built>
  <how-to-verify>
    Open each registry page in a browser and verify the package matches the source repo named in 01-RESEARCH.md §Package Legitimacy Audit. For each, confirm:
    - Author / maintainer name matches the source repo (e.g. better-auth on npm is published by the better-auth GitHub org)
    - Weekly downloads are in the high-thousands+ range (low/zero downloads = SLOP indicator)
    - No new package versions older than ~3 days (recently-published unknown versions = SUS indicator)

    Pages to check:
    - https://www.npmjs.com/package/better-auth (expect github.com/better-auth/better-auth)
    - https://www.npmjs.com/package/drizzle-orm (expect github.com/drizzle-team/drizzle-orm)
    - https://www.npmjs.com/package/@libsql/client (expect github.com/tursodatabase/libsql-client-ts)
    - https://www.npmjs.com/package/drizzle-kit (expect github.com/drizzle-team/drizzle-orm)
    - https://www.npmjs.com/package/lucide-react (expect github.com/lucide-icons/lucide)
    - https://www.npmjs.com/package/next-themes (expect github.com/pacocoursey/next-themes)
    - https://www.npmjs.com/package/sonner (expect github.com/emilkowalski/sonner)
    - https://www.npmjs.com/package/zod (expect github.com/colinhacks/zod)
  </how-to-verify>
  <acceptance_criteria>
    - User reports "approved" after manually confirming source-repo match for each package
    - No package shows as [SLOP] (impostor) or [SUS] (newly-published, low downloads)
    - If ANY package looks suspicious, user replies "blocked: <package>" — executor must stop and not run Task 3
  </acceptance_criteria>
  <resume-signal>Reply `approved` to proceed to the install task, or `blocked: <package>` to halt.</resume-signal>
  <done>User has approved all eight packages for installation.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Next.js 16 project and install verified dependencies</name>
  <files>
    - package.json
    - package-lock.json
    - tsconfig.json
    - next.config.ts
    - postcss.config.mjs
    - .gitignore
    - .env.local
    - .env.example
    - src/app/globals.css
    - src/app/layout.tsx (will be overwritten in Task 4)
    - src/app/page.tsx (will be overwritten in Task 7)
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 85-153 Standard Stack + Installation; lines 632-642 Environment Availability — pnpm is NOT installed, MUST use npm)
    - .planning/phases/01-access-shell/01-UI-SPEC.md (preset = style=new-york baseColor=zinc darkMode=true)
    - CLAUDE.md §Recommended Stack and §Installation
  </read_first>
  <action>
    The project root already exists at /Users/markwright/Development/warwick-massive-tunage with CLAUDE.md, .git, .planning, and .claude. Run `create-next-app` so it scaffolds INTO this existing directory rather than creating a sibling folder. Use these exact flags (non-interactive so no prompts block the agent): `npx create-next-app@latest . --typescript --tailwind --app --src-dir --use-npm --import-alias "@/*" --turbopack --no-eslint --no-biome --yes`. If create-next-app refuses to scaffold into a non-empty directory, pre-stash CLAUDE.md/.planning/.claude into a temp dir, scaffold, then restore them, then `git restore` anything `create-next-app` overwrote.

    Pin Next.js to ^16.2.9, React to ^19.2.0, TypeScript to ^5 (the scaffolder picks these by default for v16, but verify in package.json after install). Then install the verified package set with npm (NOT pnpm — RESEARCH A3 confirms pnpm is not installed):

    Production deps: `npm install better-auth@1.6.17 drizzle-orm@0.45.2 @libsql/client@0.17.3 zod@4.4.3 lucide-react@1.17.0 next-themes@0.4.6 sonner@2.0.7` — pin to the exact versions verified in RESEARCH.md to avoid drift.

    Dev deps: `npm install -D drizzle-kit@0.31.10`.

    Edit `next.config.ts` to add `cacheComponents: true` inside the experimental config (RESEARCH Pitfall 4 — enables `"use cache"` for Phase 3 without rework).

    Create `.gitignore` additions (if not already present): `.env.local`, `local.db`, `local.db-journal`, `drizzle/.snapshots`.

    Create `.env.local` with these keys (populate SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET from the user's reply to Task 1 — never invent values):
    - `BETTER_AUTH_SECRET=` followed by `$(openssl rand -base64 32)` output captured at this step
    - `BETTER_AUTH_URL=http://127.0.0.1:3000`
    - `NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000`
    - `SPOTIFY_CLIENT_ID=<user-supplied from Task 1>`
    - `SPOTIFY_CLIENT_SECRET=<user-supplied from Task 1>`
    - `TURSO_CONNECTION_URL=file:local.db`

    Create `.env.example` mirroring the keys with empty values plus a comment that `BETTER_AUTH_URL` MUST be `127.0.0.1` not `localhost` (RESEARCH Pitfall 1).

    DO NOT yet write `src/lib/auth.ts`, `src/app/page.tsx`, or root `layout.tsx` — those are owned by later tasks. The scaffolder will create placeholder `src/app/page.tsx` and `src/app/layout.tsx`; leave them in place for the next tasks to overwrite.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/wmt-build.log; tail -3 /tmp/wmt-build.log | grep -E "Compiled successfully|Compiled without errors"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `"next": "^16.2.9"` (or 16.2.x ≥ 16.2.9), `"react": "^19"`, `"typescript": "^5"`
    - `package.json` dependencies include: better-auth 1.6.17, drizzle-orm 0.45.2, @libsql/client 0.17.3, zod 4.4.3, lucide-react 1.17.0, next-themes 0.4.6, sonner 2.0.7
    - `package.json` devDependencies include drizzle-kit 0.31.10
    - `next.config.ts` contains the string `cacheComponents: true`
    - `.env.local` exists and contains lines starting with `BETTER_AUTH_SECRET=`, `BETTER_AUTH_URL=http://127.0.0.1:3000`, `SPOTIFY_CLIENT_ID=`, `SPOTIFY_CLIENT_SECRET=`, `TURSO_CONNECTION_URL=file:local.db`
    - `.gitignore` contains lines `.env.local` and `local.db`
    - `.env.example` exists (gitignored excluded) with the same keys but no secret values
    - `CLAUDE.md`, `.planning/`, `.claude/` survive the scaffold (re-check after create-next-app completes)
    - `npm run build` exits 0 with no errors (the placeholder pages from create-next-app suffice for this check)
  </acceptance_criteria>
  <done>Scaffold complete, dependencies installed at the pinned versions, env file populated, build passes against the placeholder pages.</done>
</task>

<task type="auto">
  <name>Task 4: Install shadcn/ui and add Phase 1 components</name>
  <files>
    - components.json
    - src/components/ui/button.tsx
    - src/components/ui/avatar.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/separator.tsx
    - src/components/ui/card.tsx
    - src/components/ui/sonner.tsx
    - src/app/globals.css (CSS variables overwritten by shadcn init)
    - tsconfig.json (path alias `@/*` confirmed by shadcn init)
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-UI-SPEC.md (entire file — preset style=new-york, baseColor=zinc, darkMode=true; component list lines 92-101; violet accent at lines 70-83)
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 109-121 shadcn components, lines 142-151 shadcn init command)
  </read_first>
  <action>
    Run `npx shadcn@latest init --yes --style new-york --base-color zinc --css-variables`. Verify `components.json` was written and the path alias resolves (it should use `@/components` and `@/lib/utils`).

    Then install the six Phase 1 components in one command: `npx shadcn@latest add button avatar badge separator card sonner --yes`.

    Open `src/app/globals.css` and override the `--primary` and `--ring` HSL values for both `:root` and `.dark` to violet-600 (`262 83% 58%` in HSL) — UI-SPEC §Color reserves violet for primary CTAs (Sign in, Start import) and focus rings. Add or confirm a `--primary-hover` variable resolving to violet-700 (`263 70% 50%`). Keep zinc-derived defaults for background/card/border per UI-SPEC §Color table.

    Confirm `tsconfig.json` has `"paths": { "@/*": ["./src/*"] }`.
  </action>
  <verify>
    <automated>test -f components.json && test -f src/components/ui/button.tsx && test -f src/components/ui/avatar.tsx && test -f src/components/ui/badge.tsx && test -f src/components/ui/separator.tsx && test -f src/components/ui/card.tsx && test -f src/components/ui/sonner.tsx && grep -q "new-york" components.json && grep -q "zinc" components.json && grep -vE "^[[:space:]]*/\*|^[[:space:]]*\*" src/app/globals.css | grep -c "262 83%" | awk '$1 > 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists; `grep -q '"style": "new-york"' components.json` succeeds; `grep -q '"baseColor": "zinc"' components.json` succeeds
    - All six component files exist under `src/components/ui/`
    - `src/app/globals.css` contains the HSL triple `262 83% 58%` for `--primary` (violet-600) — verify with `grep -v '^[[:space:]]*/\*' | grep -c '262 83%' > 0`
    - `tsconfig.json` has the `@/*` path alias mapped to `./src/*`
    - `npm run build` still exits 0
  </acceptance_criteria>
  <done>shadcn initialised with new-york/zinc/dark preset, violet accent applied to CSS variables, six Phase 1 components vendored under src/components/ui/.</done>
</task>

<task type="auto">
  <name>Task 5: Drizzle schema with Better Auth tables and role column</name>
  <files>
    - src/lib/db/index.ts
    - src/lib/db/schema.ts
    - drizzle.config.ts
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 394-414 Pattern 6; lines 478-484 Pitfall 6; lines 563-577 drizzle.config.ts example)
    - .planning/phases/01-access-shell/01-PATTERNS.md (lines 130-178 schema + db client patterns)
    - .planning/phases/01-access-shell/01-CONTEXT.md (D-05: role values are 'admin' | 'member' with default 'member')
    - node_modules/better-auth/dist/index.d.ts (after Task 3 install — confirm `additionalFields` typing)
  </read_first>
  <action>
    Create `src/lib/db/index.ts` exporting a `db` constant constructed via `drizzle({ connection: { url: process.env.TURSO_CONNECTION_URL!, authToken: process.env.TURSO_AUTH_TOKEN } })` imported from `drizzle-orm/libsql`. Pass the schema as a second argument: `drizzle({ ... }, { schema })` after the schema file exists, importing `* as schema` from `./schema`.

    Create `src/lib/db/schema.ts` with the Better Auth core tables hand-written (do NOT run `npx @better-auth/cli generate` — RESEARCH A1 flags its API as not fully verified; hand-write per documented shape to keep this plan deterministic). Use `sqliteTable` from `drizzle-orm/sqlite-core`. Required tables and columns (use these EXACT names and types — Better Auth maps them by convention):

    - `user`: id text PK, name text NOT NULL, email text NOT NULL UNIQUE, emailVerified integer NOT NULL DEFAULT 0 (boolean as int), image text NULLABLE, createdAt integer NOT NULL (timestamp_ms mode), updatedAt integer NOT NULL (timestamp_ms mode), **role text NOT NULL DEFAULT 'member'** (the D-05 additionalField column)
    - `session`: id text PK, userId text NOT NULL REFERENCES user(id) ON DELETE CASCADE, token text NOT NULL UNIQUE, expiresAt integer NOT NULL, ipAddress text NULLABLE, userAgent text NULLABLE, createdAt integer NOT NULL, updatedAt integer NOT NULL
    - `account`: id text PK, userId text NOT NULL REFERENCES user(id) ON DELETE CASCADE, accountId text NOT NULL, providerId text NOT NULL, accessToken text NULLABLE, refreshToken text NULLABLE, accessTokenExpiresAt integer NULLABLE, refreshTokenExpiresAt integer NULLABLE, scope text NULLABLE, idToken text NULLABLE, password text NULLABLE, createdAt integer NOT NULL, updatedAt integer NOT NULL
    - `verification`: id text PK, identifier text NOT NULL, value text NOT NULL, expiresAt integer NOT NULL, createdAt integer NOT NULL, updatedAt integer NOT NULL

    Export each table by name (`export const user = ...`). Do not declare relations in Phase 1 — not required by Better Auth runtime.

    Create `drizzle.config.ts` at project root with: `schema: "./src/lib/db/schema.ts"`, `out: "./drizzle"`, `dialect: "turso"`, `dbCredentials: { url: process.env.TURSO_CONNECTION_URL!, authToken: process.env.TURSO_AUTH_TOKEN }`. Wrap with `satisfies Config` imported from `drizzle-kit`.

    DO NOT run `drizzle-kit push` in this task — that is Task 8 (BLOCKING schema push). Pitfall 6 explicitly warns against pushing before the schema is complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/wmt-tsc.log; grep -E "error TS" /tmp/wmt-tsc.log | wc -l | awk '$1 == 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/schema.ts` exports four tables: `user`, `session`, `account`, `verification` — verify with `grep -E '^export const (user|session|account|verification) =' src/lib/db/schema.ts | wc -l` equals 4
    - `src/lib/db/schema.ts` contains the literal `text("role").notNull().default("member")` (D-05 — role text NOT NULL DEFAULT 'member')
    - `src/lib/db/index.ts` imports from `drizzle-orm/libsql` and exports `db`
    - `drizzle.config.ts` exists at project root with `dialect: "turso"` and `schema: "./src/lib/db/schema.ts"`
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Drizzle schema authored with Better Auth tables plus the role additionalField column, db client wired, drizzle.config.ts at project root, all type-checks pass. Ready for the BLOCKING schema push in Task 8.</done>
</task>

<task type="auto">
  <name>Task 6: Better Auth server config with Spotify provider + first-user-is-admin hook</name>
  <files>
    - src/lib/auth.ts
    - src/lib/auth-client.ts
    - src/app/api/auth/[...all]/route.ts
  </files>
  <read_first>
    - src/lib/db/schema.ts (from Task 5)
    - src/lib/db/index.ts (from Task 5)
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 237-389 Patterns 1-5; lines 508-558 client + sign-in/out examples; lines 599-606 Assumption A1 about databaseHooks)
    - .planning/phases/01-access-shell/01-PATTERNS.md (lines 33-128 auth + auth-client patterns)
    - .planning/phases/01-access-shell/01-CONTEXT.md (D-05 role values; D-06 first-user rule; D-08 Spotify scopes; D-09 Better Auth handles refresh)
    - node_modules/better-auth/dist/index.d.ts and node_modules/better-auth/dist/adapters/drizzle/index.d.ts (confirm: hooks API key name, drizzleAdapter import path, additionalFields shape)
  </read_first>
  <action>
    Create `src/lib/auth.ts`:
    - Import `betterAuth` from `better-auth` and `drizzleAdapter` from `better-auth/adapters/drizzle` (verify path against installed types — RESEARCH A5 flags this; fall back to `@better-auth/drizzle-adapter` if the types disagree).
    - Import `db` from `./db` and `* as schema` from `./db/schema`.
    - Import `sql`, `eq` from `drizzle-orm`.
    - Call `betterAuth({ ... })` with: `database: drizzleAdapter(db, { provider: "sqlite", schema })`; `socialProviders.spotify` with `clientId`/`clientSecret` from env and `scope: ["user-read-private", "user-read-email", "playlist-read-private", "playlist-read-collaborative"]` (D-08 exact list — do not abbreviate).
    - `user.additionalFields.role: { type: "string", required: false, defaultValue: "member", input: false }` (D-05; `input: false` prevents user-supplied role injection — STRIDE Elevation of Privilege mitigation).
    - `databaseHooks.user.create.after: async (user) => { ... }` implementing D-06: query `db.select({ count: sql<number>\`count(*)\` }).from(schema.user)`, if the count equals 1 (the just-created user is the first), `db.update(schema.user).set({ role: "admin" }).where(eq(schema.user.id, user.id))`. Wrap in try/catch and log to console.error on failure so an unexpected hook signature does not break the entire sign-in flow — flag the assumption with a `// ASSUMPTION A1` comment per RESEARCH Assumptions Log.
    - Export `auth` as a named const.

    Create `src/lib/auth-client.ts`:
    - Import `createAuthClient` from `better-auth/react`.
    - Export `authClient = createAuthClient({ baseURL: process.env.NEXT_PUBLIC_APP_URL ?? "http://127.0.0.1:3000" })`.

    Create `src/app/api/auth/[...all]/route.ts`:
    - Import `auth` from `@/lib/auth` and `toNextJsHandler` from `better-auth/next-js`.
    - Export `const { POST, GET } = toNextJsHandler(auth)`. No custom logic.

    Verify each file is referenced correctly via the `@/*` alias. Do NOT write `src/components/header.tsx`, `proxy.ts`, `src/app/dashboard/page.tsx`, or `src/app/api/import/route.ts` here — those belong to Plans 02 and 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/wmt-tsc2.log; grep -E "error TS" /tmp/wmt-tsc2.log | wc -l | awk '$1 == 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/auth.ts` exists and exports `auth`; `grep -c 'export const auth' src/lib/auth.ts` ≥ 1
    - `src/lib/auth.ts` contains the literal string `playlist-read-collaborative` (D-08 — the longest of the four scopes; presence confirms full list)
    - `src/lib/auth.ts` contains the literal `defaultValue: "member"` (D-05)
    - `src/lib/auth.ts` contains the literal `databaseHooks` AND a comment containing `ASSUMPTION A1`
    - `src/lib/auth-client.ts` exists and exports `authClient`
    - `src/lib/auth-client.ts` references `127.0.0.1` (no `localhost`)
    - `src/app/api/auth/[...all]/route.ts` exports both `POST` and `GET` via `toNextJsHandler`
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Better Auth wired with Spotify provider + four scopes (D-08), role additionalField (D-05), first-user-is-admin hook (D-06) with assumption flag, React client, and catch-all route handler. Type checks pass.</done>
</task>

<task type="auto">
  <name>Task 7: Public archive page and root layout shell</name>
  <files>
    - src/app/layout.tsx
    - src/app/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-UI-SPEC.md (lines 50-86 typography + color; lines 108-119 layout contract; lines 141-143 copywriting)
    - .planning/phases/01-access-shell/01-PATTERNS.md (lines 220-292 layout + page patterns)
    - .planning/phases/01-access-shell/01-CONTEXT.md (D-02 public archive renders full shell with empty state; D-04 no auth check on `/`)
    - src/app/globals.css (from Task 4 — confirm violet `--primary`)
  </read_first>
  <action>
    Overwrite `src/app/layout.tsx` (the create-next-app default) with the root layout per UI-SPEC §Layout Contract and PATTERNS lines 226-256:
    - Import `Inter` from `next/font/google` and instantiate `inter = Inter({ subsets: ["latin"] })`.
    - Import `ThemeProvider` from `next-themes`.
    - Import `Toaster` from `@/components/ui/sonner`.
    - Export `metadata: Metadata = { title: "Warwick Massive Tunage" }` (D-10).
    - Default export `RootLayout({ children })` returning `<html lang="en" suppressHydrationWarning><body className={inter.className}><ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>{children}<Toaster /></ThemeProvider></body></html>`.
    - DO NOT add a `<Header />` here yet — Plan 02 owns the header component and inserts it. Leave a `{/* Header inserted in Plan 02 */}` comment in place where the header will go.
    - Keep the `import "./globals.css"` line at the top.

    Overwrite `src/app/page.tsx` with the public archive empty-state per UI-SPEC §Public Archive (D-02, D-04, ACCESS-04):
    - No `"use client"` directive (Server Component).
    - No `"use cache"` directive in Phase 1 (RESEARCH Pitfall 4 + Anti-pattern; Phase 3 adds it when real data exists).
    - No call to `auth.api.getSession()` here — D-04 prohibits auth checks on `/`.
    - Return `<main className="mx-auto max-w-[720px] px-4 pt-16">` (max-width 720px, top padding 64px = 3xl token) containing `<h1 className="text-xl font-semibold">No sessions yet</h1>` and `<p className="mt-2 text-base text-muted-foreground">Sessions will appear here once the archive has been imported. Check back soon.</p>` (copy verbatim from UI-SPEC line 142-143).

    Confirm `src/app/globals.css` still contains the Tailwind directives (`@import "tailwindcss"` for v4) and the violet `--primary` override from Task 4.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/wmt-build7.log; tail -5 /tmp/wmt-build7.log | grep -E "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` imports `next/font/google` AND `next-themes` AND `@/components/ui/sonner`
    - `src/app/layout.tsx` contains the literal `defaultTheme="dark"` and `enableSystem={false}`
    - `src/app/layout.tsx` does NOT import or render a Header component yet (Plan 02 owns that)
    - `src/app/page.tsx` does NOT contain the strings `getSession`, `redirect(`, `auth.`, `headers()`, or `"use cache"` (D-04 — no auth on public route)
    - `src/app/page.tsx` contains both literal strings: `No sessions yet` AND `Sessions will appear here once the archive has been imported. Check back soon.` (UI-SPEC line 142-143)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Public archive empty-state page renders without auth; root layout wired with dark-by-default ThemeProvider + Sonner toaster, with a clear seam for Plan 02 to insert the header.</done>
</task>

<task type="auto">
  <name>Task 8: [BLOCKING] Push schema to local SQLite</name>
  <files>
    - drizzle/ (migration snapshot directory created by drizzle-kit)
    - local.db (created at project root by libSQL driver on first connect)
  </files>
  <read_first>
    - drizzle.config.ts (from Task 5)
    - src/lib/db/schema.ts (from Task 5)
    - .planning/phases/01-access-shell/01-RESEARCH.md (lines 478-484 Pitfall 6 — schema must be merged BEFORE push)
  </read_first>
  <action>
    Run `npx drizzle-kit push` from the project root. This is the BLOCKING schema push required by `<schema_push_requirement>` — without it, the database has no tables and Better Auth will fail at the first `db.select` call even though build and type-check pass (the types come from schema.ts, not the live database).

    drizzle-kit push will:
    1. Read schema.ts via drizzle.config.ts.
    2. Connect to `file:local.db` (created on first connect — no Turso account needed for Phase 1).
    3. Emit CREATE TABLE statements for `user`, `session`, `account`, `verification` (including the `role` column on `user` with `DEFAULT 'member'`).
    4. The command is non-interactive when pushing to an empty database. If it prompts about any "data loss" warning (it should not for a fresh DB), abort and investigate Pitfall 6.

    After the push, verify the tables exist by running a one-line probe: `node -e "const { drizzle } = require('drizzle-orm/libsql'); const { sql } = require('drizzle-orm'); const db = drizzle({ connection: { url: 'file:local.db' } }); db.run(sql\`SELECT name FROM sqlite_master WHERE type='table'\`).then(r => { console.log(JSON.stringify(r.rows)); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });"` and confirm the output contains all four table names.

    Confirm the `role` column exists with the correct default by running `sqlite3 local.db "SELECT sql FROM sqlite_master WHERE name='user';"` and checking the CREATE TABLE statement contains `role TEXT NOT NULL DEFAULT 'member'`.

    Note for the executor: this task is marked autonomous but is `[BLOCKING]` in the plan-level sense — verification of the phase CANNOT pass until this push has run successfully. If `sqlite3` CLI is not available, fall back to the Node probe above; both succeed proves the schema is live.
  </action>
  <verify>
    <automated>test -f local.db && node -e "const { drizzle } = require('drizzle-orm/libsql'); const { sql } = require('drizzle-orm'); const db = drizzle({ connection: { url: 'file:local.db' } }); db.run(sql\`SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\`).then(r => { const names = r.rows.map(x => x.name).join(','); if (names.includes('user') && names.includes('session') && names.includes('account') && names.includes('verification')) { process.exit(0); } else { console.error('missing tables, got:', names); process.exit(1); } });"</automated>
  </verify>
  <acceptance_criteria>
    - `local.db` exists at project root (sized > 0 bytes)
    - The four tables (`user`, `session`, `account`, `verification`) exist in `local.db`
    - The `user` table's CREATE statement contains both `role` AND `'member'` (the D-05 default)
    - `drizzle-kit push` exited 0 with no "data loss" warnings
  </acceptance_criteria>
  <done>Local SQLite database has the full schema live and ready for Better Auth to write user/session/account rows during the OAuth flow that Plan 02 exercises.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Verify dev server boots and public archive renders</name>
  <read_first>
    - .planning/phases/01-access-shell/01-UI-SPEC.md (§Public Archive — empty state appearance)
    - .planning/phases/01-access-shell/01-CONTEXT.md (D-04 public route must be reachable without auth)
  </read_first>
  <files>none — human-verify of the dev server boot; no files modified by this task</files>
  <action>Pause execution. Present the boot + browse steps from `<how-to-verify>` to the user. Wait for them to run `npm run dev` and confirm the public archive renders and `/api/auth/session` returns JSON. If the user replies `approved`, mark Plan 01 complete. If the user replies `issue: <description>`, halt and surface the issue to the orchestrator for revision planning.</action>
  <verify>
    <human-check>User has confirmed `npm run dev` boots cleanly on http://127.0.0.1:3000, the public empty-state page renders at `/`, and `/api/auth/session` returns a JSON response.</human-check>
  </verify>
  <what-built>Plan 01 has scaffolded the Next.js project, installed the verified dependency set, configured Better Auth (Spotify provider + role + first-user-admin hook), wired Drizzle to local SQLite, pushed the schema, and built the public archive empty-state page. This checkpoint verifies the Walking Skeleton boots end-to-end before Plan 02 layers on auth UI.</what-built>
  <how-to-verify>
    1. In a separate terminal, run `npm run dev`.
    2. Wait for the "Ready" line (should mention `http://127.0.0.1:3000`).
    3. In a browser, open `http://127.0.0.1:3000/` (use 127.0.0.1 explicitly, NOT localhost — RESEARCH Pitfall 1).
    4. Confirm you see: a dark page (zinc-950 background) with the heading "No sessions yet" and the body "Sessions will appear here once the archive has been imported. Check back soon." (UI-SPEC §Public Archive). No header is present yet — Plan 02 adds it.
    5. In the same browser, open `http://127.0.0.1:3000/api/auth/session`. Expect a JSON response with `null` or `{"session":null,"user":null}` (proves Better Auth route handler is mounted; not signed in yet so no session).
    6. In the terminal where dev is running, look at the request log — no 500 errors should have occurred.
    7. Stop the dev server with Ctrl+C.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run dev` outputs "Ready" with `http://127.0.0.1:3000` and no error stack
    - Browsing `http://127.0.0.1:3000/` returns 200 and renders the empty-state copy (UI-SPEC line 142-143)
    - Browsing `http://127.0.0.1:3000/api/auth/session` returns 200 with a JSON body (not a 404 or 500)
    - No 500 errors appear in the dev console during these checks
    - User confirms the page appears dark-themed (proves Tailwind v4 + ThemeProvider dark default + zinc-950 background are wired)
  </acceptance_criteria>
  <resume-signal>Reply `approved` to mark Plan 01 complete and unblock Plan 02. Reply `issue: <description>` if something is wrong; the executor will diagnose and revise.</resume-signal>
  <done>Walking Skeleton foundation boots end-to-end. Plan 02 can now wire the OAuth UI on top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js server | Untrusted HTTP requests cross into the server: requests to `/`, `/api/auth/*`, and any future API route. Cookies (including the Better Auth session cookie) are user-controllable. |
| Next.js server → Spotify OAuth | Outbound — but the redirect URL Spotify returns to is user-influenceable; the OAuth `state` parameter must be validated. |
| Next.js server → libSQL (local file in Phase 1) | In-process driver, no network boundary in dev. Production crosses the network to Turso. |
| Better Auth `databaseHooks.user.create.after` callback context | A trust point: this callback runs on the server and mutates the user table; its input is the just-created user record from Better Auth, not raw user input — but a bug here can promote the wrong user to admin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Spotify OAuth callback (`/api/auth/callback/spotify`) | mitigate | Better Auth handles PKCE + state verification automatically (RESEARCH §Don't Hand-Roll). Redirect URI is locked to `http://127.0.0.1:3000/api/auth/callback/spotify` in the Spotify dashboard (Task 1 acceptance criteria) — Spotify rejects mismatched redirects. |
| T-01-02 | Tampering | `.env.local` file containing `BETTER_AUTH_SECRET` and `SPOTIFY_CLIENT_SECRET` | mitigate | `.env.local` is added to `.gitignore` in Task 3 acceptance criteria. `.env.example` (with empty values) committed instead. RESEARCH §Security Domain — never commit secrets. |
| T-01-03 | Repudiation | First-user-is-admin promotion | accept | Mark always sets up first per CONTEXT.md §Specifics. If the wrong user logs in first, manual SQL UPDATE on `user.role` fixes it — acceptable for a 4-person team. Logged via console.error in the hook (Task 6 action). |
| T-01-04 | Information Disclosure | Spotify access/refresh tokens stored in `account` table | mitigate | Tokens live in the `account` table server-side only. `account.accessToken`/`refreshToken` columns are never exposed to the browser. Better Auth's session API returns user data without raw provider tokens (RESEARCH §Security Domain). |
| T-01-05 | Denial of Service | Public `/` route | accept | D-04 mandates an open public route. Phase 1 has no DB read on `/` (pure static empty state). Future caching with `"use cache"` (Phase 3) is the standard mitigation; the `cacheComponents: true` flag is already enabled in `next.config.ts` (Task 3) to make that future work zero-cost. |
| T-01-06 | Elevation of Privilege | Role injection via OAuth profile (user supplies their own `role` field) | mitigate | `user.additionalFields.role` is configured with `input: false` (D-05, Task 6 action) — Better Auth ignores any client-supplied role value. Default is `'member'`. |
| T-01-07 | Elevation of Privilege | `databaseHooks` callback misuse promoting a non-first user to admin | mitigate | Hook counts existing `user` rows via `SELECT count(*) FROM user` AFTER the row insert; promotes ONLY when count === 1 (D-06). Hook wrapped in try/catch so a hook crash never blocks the sign-in flow (Task 6 action). |
| T-01-SC | Tampering | npm install of better-auth, drizzle-orm, @libsql/client, drizzle-kit, lucide-react, next-themes, sonner, zod | mitigate | slopcheck was unavailable at research time — all packages tagged `[ASSUMED]` in RESEARCH.md §Package Legitimacy Audit. Task 2 is a `checkpoint:human-verify` with `gate="blocking-human"` requiring the developer to confirm each package's registry page matches its source repo before Task 3 installs them. Versions are pinned to the exact strings RESEARCH verified on npm registry (no `^` or `~` ranges in the install command), preventing the executor from drifting onto a later compromised version. |
</threat_model>

<verification>
- `npm run build` exits 0 with no errors after Tasks 3, 4, 5, 6, 7 (verified by automated checks in each task)
- `npx tsc --noEmit` exits with zero `error TS` lines after Tasks 5 and 6
- `local.db` exists at project root with all four tables AND the `role` column with default `'member'` (Task 8)
- `npm run dev` boots cleanly; `http://127.0.0.1:3000/` returns 200 with the empty-state copy (Task 9 human-verify)
- `http://127.0.0.1:3000/api/auth/session` returns a 200 JSON response (proves Better Auth catch-all route handler is mounted)
- `.env.local` is in `.gitignore`; `.env.example` is not
</verification>

<success_criteria>
Plan 01 is complete when:
1. The Next.js 16 + TypeScript + Tailwind v4 + shadcn/ui project exists with the verified dependency set installed at pinned versions.
2. `src/lib/auth.ts` is configured with the Spotify social provider requesting the four D-08 scopes, the `role` additionalField (D-05), and the first-user-is-admin databaseHook (D-06) — all in one file ready for Plan 02 to consume.
3. The local SQLite database has the full schema live (user, session, account, verification) with the role column defaulting to 'member'.
4. The public archive empty-state page at `/` renders without any auth check (D-04, ACCESS-04) and shows the exact UI-SPEC copy.
5. The Better Auth catch-all route handler at `/api/auth/[...all]/route.ts` responds to `/api/auth/session` with a JSON body.
6. The dev server boots and the public route is browsable end-to-end at `http://127.0.0.1:3000/` (human-verified in Task 9).

Plan 01 satisfies the Walking Skeleton requirement of "one real DB read AND one real write" via the `databaseHooks.user.create.after` callback (set up in Task 6, exercised end-to-end in Plan 02's OAuth flow). Plan 01 does NOT yet satisfy ACCESS-02 or ACCESS-03 — those need the dashboard page and import route that Plans 02 and 03 add.
</success_criteria>

<output>
Create `.planning/phases/01-access-shell/01-01-SUMMARY.md` when done, recording: scaffold command actually used, npm versions installed (resolve `^` ranges to actual versions from package-lock.json), any deviation from the planned file list, the actual cookie name discovered if Task 9 surfaces it, and any A1/A2/A5 assumption resolutions (whether `databaseHooks` API matched the assumed shape, whether the drizzle adapter import path was `better-auth/adapters/drizzle` as planned).
</output>
