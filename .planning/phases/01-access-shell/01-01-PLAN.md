---
phase: 01-access-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - biome.json
  - components.json
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/avatar.tsx
  - components/ui/badge.tsx
  - components/ui/separator.tsx
  - components/ui/sonner.tsx
  - lib/utils.ts
  - .env.local.example
  - .gitignore
autonomous: false
requirements: []
tags:
  - nextjs
  - tailwind
  - shadcn
  - scaffold

user_setup:
  - service: "Local toolchain"
    why: "pnpm is not installed on this machine; must be installed globally before scaffold"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "pnpm --version reports a 11.x version on PATH"
    - "pnpm dev boots Next.js 16 on http://localhost:3000 with the default page rendering"
    - "Tailwind v4 utility classes (e.g. bg-zinc-950) apply on the rendered page"
    - "shadcn Button component imported from @/components/ui/button compiles and renders"
    - "Biome lint/format command runs without crashing"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16.2.9, React 19.2, TypeScript 5.x, Tailwind v4.3, Better Auth 1.6.17, Drizzle 0.45.2, libSQL 0.17.3, drizzle-kit 0.31.10, zod 4.x, biome 2.5.0, lucide-react, sonner deps"
      contains: "\"next\": \"16."
    - path: "components.json"
      provides: "shadcn config: new-york style, zinc base, dark mode default, @/components alias"
      contains: "\"style\": \"new-york\""
    - path: "components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "components/ui/avatar.tsx"
      provides: "shadcn Avatar component"
    - path: "components/ui/badge.tsx"
      provides: "shadcn Badge component"
    - path: "components/ui/separator.tsx"
      provides: "shadcn Separator component"
    - path: "components/ui/sonner.tsx"
      provides: "shadcn Sonner toast component"
    - path: "lib/utils.ts"
      provides: "cn() utility used by shadcn components"
      contains: "export function cn"
    - path: "biome.json"
      provides: "Biome lint + format config"
    - path: "app/globals.css"
      provides: "Tailwind v4 @import directives and shadcn css variables for zinc/dark"
      contains: "@import \"tailwindcss\""
    - path: ".env.local.example"
      provides: "Example env vars: BETTER_AUTH_SECRET, BETTER_AUTH_URL, NEXT_PUBLIC_APP_URL, DATABASE_URL"
      contains: "BETTER_AUTH_SECRET"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "components/ui/button.tsx"
      to: "lib/utils.ts"
      via: "cn helper import"
      pattern: "from .@/lib/utils."
---

<objective>
Lay down the Phase 1 Walking Skeleton scaffold: install pnpm globally, scaffold the Next.js 16 + Tailwind v4 project, install all verified Phase 1 runtime + dev dependencies, install the six shadcn/ui components the UI-SPEC requires, and verify the dev server boots. NO database, auth, or routing logic yet — that lands in Plan 01-01b.

Purpose: Establishes the build toolchain and design-system baseline that every subsequent plan depends on. After this plan, the project compiles, the dev server runs, and the UI primitives the rest of Phase 1 needs are present.

Output: A buildable Next.js 16 project with Tailwind v4, Biome, the six shadcn components from UI-SPEC, and example env files. No runtime behavior beyond the default Next.js starter page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-shell/01-CONTEXT.md
@.planning/phases/01-access-shell/01-RESEARCH.md
@.planning/phases/01-access-shell/01-PATTERNS.md
@.planning/phases/01-access-shell/01-UI-SPEC.md
@.planning/phases/01-access-shell/SKELETON.md
@CLAUDE.md

<interfaces>
<!-- No prior plan output. All scaffolding is greenfield. -->
<!-- Versions are LOCKED per RESEARCH.md §Standard Stack (npm registry verified 2026-06-12). -->

Locked package versions:
- next@16.2.9
- react@19.2.x (auto-resolved by next)
- typescript@5.x (auto-resolved by create-next-app)
- tailwindcss@4.3.0
- better-auth@1.6.17               # used in next plan
- drizzle-orm@0.45.2                # used in next plan
- @libsql/client@0.17.3             # used in next plan
- drizzle-kit@0.31.10                # used in next plan
- zod@4.x                           # used in next plan for sign-in form
- @biomejs/biome@2.5.0
- lucide-react@1.18.0
- sonner (latest — required by shadcn add sonner CLI)
- pnpm@11.6.0 (installed globally)

shadcn preset (UI-SPEC §Design System):
- style: new-york
- baseColor: zinc
- darkMode: enabled (default)
- cssVariables: true
- aliases: @/components, @/lib/utils
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: User prereq — install pnpm globally</name>
  <what-built>This task is the only step Claude cannot fully automate without a global npm permission grant. RESEARCH.md confirms pnpm is not installed on this machine (verified Pitfall 4).</what-built>
  <how-to-verify>
    Run from any terminal:

    ```
    npm install -g pnpm@latest
    pnpm --version
    ```

    Expected: `pnpm --version` prints a version starting with `11.` (or newer). If you see "command not found" or a permissions error, re-run with `sudo` or fix npm's global prefix per your environment.

    Once `pnpm --version` succeeds, type "approved" to resume. If you would rather use npm throughout (CLAUDE.md prefers pnpm but npm works), type "use-npm" and Claude will substitute `npm` for `pnpm` in every subsequent command.
  </how-to-verify>
  <resume-signal>Type "approved" (pnpm installed) or "use-npm" (substitute npm). The remaining tasks assume pnpm — if you choose "use-npm", every `pnpm` in this plan becomes `npm` and every `pnpm dlx` becomes `npx`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 16 + Tailwind v4 + install full dep set + Biome</name>
  <files>
    package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, postcss.config.mjs,
    app/layout.tsx, app/page.tsx, app/globals.css, biome.json,
    .env.local.example, .gitignore
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-RESEARCH.md §Standard Stack (line 76-128)
    - .planning/phases/01-access-shell/01-RESEARCH.md §Package Legitimacy Audit (line 130-152)
    - .planning/phases/01-access-shell/01-RESEARCH.md §Environment Availability (line 596-606)
    - .planning/phases/01-access-shell/01-PATTERNS.md §Environment Variables Pattern (line 533-543)
    - CLAUDE.md §Recommended Stack and §Installation
  </read_first>
  <action>
    Scaffold the project IN PLACE in the existing working directory (`/Users/markwright/Development/warwick-massive-tunage`). The directory currently contains only `CLAUDE.md` and `.planning/` — both must be preserved.

    Step 1 — Scaffold (D-AUTH, D-01, D-02 inform the structure):

    Run `pnpm create next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*" --use-pnpm --skip-install --yes`.

    The trailing `.` tells create-next-app to scaffold into the current directory. `--no-src-dir` matches RESEARCH.md §Recommended Project Structure (App Router at `app/`, not `src/app/`). `--skip-install` lets us add the rest of the deps before a single install. If create-next-app refuses to write into a non-empty directory, scaffold to `./.next-scaffold-tmp` and then move files out and remove the tmp dir, preserving `CLAUDE.md` and `.planning/` untouched.

    Pin Next.js to exact version: edit `package.json` to set `"next": "16.2.9"` (no caret) so future installs are reproducible while we are still on the Walking Skeleton. Set `"react": "19.2"` and `"react-dom": "19.2"` similarly.

    Step 2 — Install Phase 1 runtime deps (locked versions from RESEARCH.md):

    ```
    pnpm add \
      next@16.2.9 \
      better-auth@1.6.17 \
      drizzle-orm@0.45.2 \
      @libsql/client@0.17.3 \
      zod@4 \
      lucide-react@1.18.0 \
      sonner
    ```

    Step 3 — Install dev deps:

    ```
    pnpm add -D \
      drizzle-kit@0.31.10 \
      @biomejs/biome@2.5.0 \
      typescript@5
    ```

    Step 4 — Pin Tailwind to v4.3.0:

    Confirm `tailwindcss@4.3.0` and `@tailwindcss/postcss` are present in `package.json` (added by create-next-app via `--tailwind`). If a different Tailwind v4 patch was installed, run `pnpm add -D tailwindcss@4.3.0 @tailwindcss/postcss`. Ensure `postcss.config.mjs` uses the v4 `@tailwindcss/postcss` plugin (created by scaffold — verify it is `{ plugins: { '@tailwindcss/postcss': {} } }`).

    Step 5 — Initialise Biome:

    Run `pnpm dlx @biomejs/biome@2.5.0 init`. This creates `biome.json` with sensible defaults. Then edit `biome.json` so `formatter.indentStyle` is `"space"`, `linter.rules.recommended` is `true`, and `files.ignore` includes `["node_modules", ".next", "drizzle", "*.config.*"]`. Add scripts to `package.json`:

    - `"lint": "biome check ."`
    - `"format": "biome format --write ."`
    - `"typecheck": "tsc --noEmit"`

    Remove any ESLint config or `next lint` script if create-next-app added them — `next lint` was removed in Next.js 16.

    Step 6 — Update `next.config.ts`:

    Keep create-next-app defaults. Ensure the file exports a typed `NextConfig` object. No experimental flags are required for Phase 1. Do NOT enable `experimental.cacheComponents` yet — the public `/` does not use `"use cache"` in Phase 1 (deferred to Phase 3 per SKELETON.md).

    Step 7 — Write `.env.local.example` (do NOT commit `.env.local` itself — `.gitignore` already excludes it via create-next-app):

    Contents:
    ```
    # Better Auth — generate with: openssl rand -base64 32
    BETTER_AUTH_SECRET=replace-me
    BETTER_AUTH_URL=http://localhost:3000
    NEXT_PUBLIC_APP_URL=http://localhost:3000

    # Local SQLite for dev; Turso libSQL URL + auth token for prod
    DATABASE_URL=file:local.db
    # DATABASE_AUTH_TOKEN=        # only required for Turso production
    ```

    Step 8 — Create an actual `.env.local` with a generated secret so subsequent plans can boot:

    ```
    SECRET=$(openssl rand -base64 32)
    cat > .env.local <<EOF
    BETTER_AUTH_SECRET=$SECRET
    BETTER_AUTH_URL=http://localhost:3000
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    DATABASE_URL=file:local.db
    EOF
    ```

    Step 9 — Append to `.gitignore` (preserve existing lines):

    ```
    # Local dev DB (libSQL/SQLite)
    local.db
    local.db-journal
    *.db-shm
    *.db-wal

    # Drizzle migrations are committed; generated SQL artifacts go through drizzle/ in the next plan
    ```

    Step 10 — Sanity boot:

    Run `pnpm install` to fully sync the lockfile, then `pnpm dev` in the background (timeout 25s). The dev server should print `Ready in ...` and bind to `http://localhost:3000`. Kill the background process before continuing. Do NOT leave a dev server running after the task.
  </action>
  <verify>
    <automated>
      test -f package.json &&
      test -f next.config.ts &&
      test -f app/layout.tsx &&
      test -f app/page.tsx &&
      test -f app/globals.css &&
      test -f biome.json &&
      test -f .env.local &&
      test -f .env.local.example &&
      grep -q '"next": "16.2.9"' package.json &&
      grep -q '"better-auth": "1.6.17"' package.json &&
      grep -q '"drizzle-orm": "0.45.2"' package.json &&
      grep -q '"@libsql/client": "0.17.3"' package.json &&
      grep -q '"drizzle-kit": "0.31.10"' package.json &&
      grep -q '"@biomejs/biome": "2.5.0"' package.json &&
      grep -q '"tailwindcss": "4.3.0"' package.json &&
      grep -q '"zod"' package.json &&
      grep -q '"sonner"' package.json &&
      grep -q '"lucide-react"' package.json &&
      grep -q 'BETTER_AUTH_SECRET=' .env.local.example &&
      grep -q '^BETTER_AUTH_SECRET=' .env.local &&
      test -d node_modules &&
      pnpm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists all 10 locked packages at the pinned versions
    - `pnpm install` completes with no peer-dep errors
    - `pnpm run typecheck` exits 0
    - `.env.local` contains a real (non-placeholder) `BETTER_AUTH_SECRET` value
    - `.env.local` is NOT tracked by git (check `git check-ignore .env.local`)
    - No `middleware.ts`, no `eslint.config.*`, no `next lint` script in package.json
  </acceptance_criteria>
  <done>Project compiles, lockfile is reproducible, env scaffolding is in place, Biome is the lint/format tool of record.</done>
</task>

<task type="auto">
  <name>Task 3: Install shadcn/ui (new-york / zinc / dark) and add the six Phase 1 components</name>
  <files>
    components.json, lib/utils.ts, app/globals.css, app/layout.tsx,
    components/ui/button.tsx, components/ui/card.tsx, components/ui/avatar.tsx,
    components/ui/badge.tsx, components/ui/separator.tsx, components/ui/sonner.tsx
  </files>
  <read_first>
    - .planning/phases/01-access-shell/01-UI-SPEC.md §Design System (line 16-29)
    - .planning/phases/01-access-shell/01-UI-SPEC.md §Color (line 65-86)
    - .planning/phases/01-access-shell/01-UI-SPEC.md §Component Inventory (line 90-104)
    - .planning/phases/01-access-shell/01-UI-SPEC.md §Typography (line 50-62)
  </read_first>
  <action>
    Step 1 — Init shadcn (UI-SPEC §Design System):

    Run `pnpm dlx shadcn@latest init --base-color zinc --css-variables --yes`. When the interactive prompts fall through `--yes`, the CLI will write:

    - `components.json` (style `new-york`, baseColor `zinc`, cssVariables `true`, aliases `@/components` and `@/lib/utils`)
    - `lib/utils.ts` with the `cn()` helper
    - Tailwind-compatible CSS variable definitions injected into `app/globals.css`

    If the CLI prompts for style, choose `new-york`. If it asks about React Server Components, answer yes. If it asks about dark mode, choose `default` (we set `defaultTheme` to dark via the body class — see Step 4 below).

    Verify `components.json` contains:
    ```json
    {
      "style": "new-york",
      "tailwind": { "baseColor": "zinc", "cssVariables": true },
      "aliases": { "components": "@/components", "utils": "@/lib/utils" }
    }
    ```

    Step 2 — Add the six UI-SPEC components in a single CLI run:

    ```
    pnpm dlx shadcn@latest add button card avatar badge separator sonner --yes
    ```

    This writes:
    - `components/ui/button.tsx`
    - `components/ui/card.tsx`
    - `components/ui/avatar.tsx`
    - `components/ui/badge.tsx`
    - `components/ui/separator.tsx`
    - `components/ui/sonner.tsx`

    Step 3 — UI-SPEC accent override (`violet-600` primary, see UI-SPEC §Color lines 67-86):

    shadcn's zinc base sets `--primary` to a near-black. Per UI-SPEC the **accent** for buttons + focus rings is `violet-600 (#7c3aed)`. Override the `--primary` and `--ring` CSS variables in `app/globals.css` inside the `.dark` (or `:root.dark` / `:root` if dark is the default) block to:

    ```css
    --primary: oklch(0.554 0.252 296);   /* violet-600 */
    --primary-foreground: oklch(0.985 0 0); /* near-white */
    --ring: oklch(0.554 0.252 296);
    ```

    Do NOT change the other zinc tokens. If the CLI emitted the variables in HSL or hex, match whichever format is present — convert violet-600 (#7c3aed) accordingly. The goal is: shadcn Button `variant="default"` renders violet on the dashboard.

    Step 4 — Wire Inter font + dark mode default in `app/layout.tsx` (UI-SPEC §Typography, §Color "dark mode default"):

    Replace the existing `app/layout.tsx` body with:

    ```tsx
    import type { Metadata } from "next";
    import { Inter } from "next/font/google";
    import { Toaster } from "@/components/ui/sonner";
    import "./globals.css";

    const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

    export const metadata: Metadata = {
      title: "Warwick Massive Tunage",
      description: "31 sessions of curated music",
    };

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en" className="dark" suppressHydrationWarning>
          <body className={`${inter.variable} font-sans bg-background text-foreground antialiased`}>
            {children}
            <Toaster richColors position="top-center" />
          </body>
        </html>
      );
    }
    ```

    The `className="dark"` on `<html>` is the simplest "always dark" approach — no `next-themes` package is needed for Phase 1. The Toaster is mounted here so the sign-in error toast (UI-SPEC §Interaction States, Plan 01-02) works site-wide.

    Step 5 — Tell Tailwind about the Inter CSS variable. In `app/globals.css`, ensure the font-sans utility maps to `var(--font-inter)`. Tailwind v4 CSS-first config: inside `@theme { ... }` (created by shadcn init or `tailwindcss` import), add:

    ```css
    @theme {
      --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
    }
    ```

    Step 6 — Quick smoke render. Edit `app/page.tsx` to a temporary smoke screen that imports Button + Card so TypeScript catches any path-alias mistake. This file will be REPLACED entirely by Plan 01-01b's public archive — that's fine, the smoke test is throwaway:

    ```tsx
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";

    export default function Home() {
      return (
        <main className="p-8">
          <Card className="p-6 max-w-sm">
            <h1 className="text-xl font-semibold">Skeleton ready</h1>
            <Button className="mt-4">Smoke test</Button>
          </Card>
        </main>
      );
    }
    ```
  </action>
  <verify>
    <automated>
      test -f components.json &&
      test -f lib/utils.ts &&
      test -f components/ui/button.tsx &&
      test -f components/ui/card.tsx &&
      test -f components/ui/avatar.tsx &&
      test -f components/ui/badge.tsx &&
      test -f components/ui/separator.tsx &&
      test -f components/ui/sonner.tsx &&
      grep -q '"style": "new-york"' components.json &&
      grep -q '"baseColor": "zinc"' components.json &&
      grep -q 'export function cn' lib/utils.ts &&
      grep -q 'className="dark"' app/layout.tsx &&
      grep -q 'next/font/google' app/layout.tsx &&
      grep -q 'Toaster' app/layout.tsx &&
      pnpm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `components.json` declares style=new-york, baseColor=zinc, cssVariables=true
    - `lib/utils.ts` exports `cn(...inputs)` for Tailwind class merging
    - All six shadcn components (button, card, avatar, badge, separator, sonner) exist under `components/ui/`
    - `app/layout.tsx` mounts Inter font, sets `<html className="dark">`, and renders the Sonner Toaster site-wide
    - `app/globals.css` overrides `--primary` and `--ring` to a violet value (UI-SPEC accent)
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>shadcn/ui is initialised with the locked UI-SPEC preset, the six Phase 1 components are present, and the root layout is configured for dark mode + Inter font + Sonner toasts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A buildable Next.js 16 + Tailwind v4 + shadcn/ui scaffold. The dev server boots, the smoke-test Button renders, and the toolchain is ready for the Plan 01-01b wiring step.
  </what-built>
  <how-to-verify>
    1. Open a terminal in the project root and run `pnpm dev`.
    2. Wait for `Ready in ...` then open `http://localhost:3000`.
    3. Confirm the page renders a Card containing the heading "Skeleton ready" and a "Smoke test" Button.
    4. Confirm the page is **dark** (zinc-950 background, near-black) and the Button is **violet** (not zinc).
    5. Confirm the font is Inter — `view source` and check the `<html>` has the inter CSS variable applied to the body.
    6. Press Ctrl+C to stop the dev server.
    7. Run `pnpm run lint` — Biome should run and report zero errors (warnings are fine).
    8. Run `pnpm run typecheck` — TypeScript should exit 0.

    If the button is the default zinc near-black (not violet), the `--primary` CSS variable override in Task 3 Step 3 did not take effect — re-run Step 3.
  </how-to-verify>
  <resume-signal>Type "approved" if dev server boots and the smoke screen renders with the dark theme + violet Button. If something else, describe what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem ↔ npm registry | Package install pulls third-party JS from registry.npmjs.com — supply-chain surface |
| Developer terminal ↔ npm global prefix | `npm install -g pnpm` writes to a privileged path on the dev machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package install (next, better-auth, drizzle, @libsql/client, tailwindcss, biome, drizzle-kit, lucide-react, sonner, zod) | mitigate | All packages tagged [ASSUMED OK] in RESEARCH.md §Package Legitimacy Audit; manual checkpoint Task 1 means a human kicks off the install; RESEARCH.md verified no postinstall scripts present on any of the listed packages; lockfile pinned via exact versions in package.json |
| T-01-02 | Tampering | shadcn CLI `pnpm dlx shadcn@latest` downloads + writes component source | accept | shadcn `dlx` writes plain React + Tailwind code under `components/ui/`; no postinstall hooks; output is reviewed by Claude in Task 3 verify step |
| T-01-03 | Information Disclosure | `.env.local` containing `BETTER_AUTH_SECRET` | mitigate | `.gitignore` excludes `.env.local` (Task 2 Step 9 verifies via `git check-ignore`); committed `.env.local.example` carries no real secrets |
| T-01-04 | Elevation of Privilege | Global pnpm install via `npm install -g` | accept | User-initiated checkpoint (Task 1) — only the user can authorise a global install on their machine; not a runtime threat |
| T-01-SC | Tampering | npm/pnpm installs (supply-chain) | mitigate | Locked versions verified against npm registry on 2026-06-12; no [SLOP] or [SUS] packages; Task 1 is `gate="blocking-human"` per slopcheck fallback policy |
</threat_model>

<verification>
After this plan, the following must hold:

- `pnpm dev` starts the Next.js 16 dev server on http://localhost:3000 within 15 seconds
- The smoke page renders with dark zinc background and a violet Button
- `pnpm run typecheck` exits 0
- `pnpm run lint` exits 0 (Biome)
- All Phase 1 locked package versions are in `package.json` at the pinned versions
- `.env.local` contains a generated `BETTER_AUTH_SECRET` and is gitignored
- No `middleware.ts`, no ESLint config, no `eslint.json` file
</verification>

<success_criteria>
1. Next.js 16 project compiles end-to-end (typecheck passes, lint passes, dev server boots)
2. Tailwind v4 utility classes apply on a rendered page
3. shadcn/ui (new-york/zinc/dark, violet primary, Inter font) is installed with all six Phase 1 components
4. Environment scaffolding (`.env.local` with secret, `.env.local.example` committed, gitignore correct) is in place for the next plan
5. Biome is the lint/format tool of record — no leftover ESLint
6. Human checkpoint approved (Task 1 prereq) and human verify approved (final smoke check)
</success_criteria>

<output>
Create `.planning/phases/01-access-shell/01-01-SUMMARY.md` when done.

SUMMARY must include:
- Confirmation of all locked versions installed
- Path to `.env.local.example` (committed) and `.env.local` (gitignored)
- Confirmation of dark-mode + violet override applied
- Patterns established: `@/*` import alias, `cn()` helper from `@/lib/utils`, shadcn `components/ui/` location
- Any deviations from the locked versions (none expected — flag if any)
</output>
