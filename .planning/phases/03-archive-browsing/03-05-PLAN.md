---
phase: 03-archive-browsing
plan: 05
type: execute
wave: 2
depends_on: ["03-04"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [BROWSE-03]

must_haves:
  truths:
    - "The admin has edited the Apple Music playlist descriptions for every fallback-track session (3, 9, 15, 19, 31, and the S24 Iron Man/Rahzel case) to the canonical format \"<Name>'s <descriptor> track: <Artist> - <Title> <url>\", adding a URL to S24"
    - "After a MusicKit re-import, each fallback track exists as its own track attributed to the named contributor"
    - "S3 shows 2 fallback tracks, each with a YouTube link and no Apple Music link"
    - "S31 shows Jonny's diamond track (DND - Diamond Rings) under JS with a YouTube link and no Apple Music link"
    - "No fallback YouTube link is attached to the wrong (Apple Music) track — in particular Iwan's S31 position-1 Grace Jones track no longer carries a YouTube link"
  artifacts: []
  key_links:
    - from: "Apple Music playlist descriptions"
      to: "parseFallbackTracks (via re-import)"
      via: "admin edits descriptions to canonical format, then re-imports through MusicKit"
      pattern: "re-import"
---

<objective>
Land the human half of the fallback-track fix. Plan 03-04 shipped the parser + import-route changes, but they only take effect against a live database after (a) the admin updates the Apple Music playlist descriptions to the canonical fallback-track format, and (b) a MusicKit re-import repopulates the archive. This is the same "data-layer changes only take effect after a destructive re-import" pattern established in 03-01-SUMMARY — build/typecheck passing is NOT sufficient proof; the live DB must be re-imported and spot-checked.

Canonical format (authoritative, from 03-UAT.md resolution):

  "<Name>'s <descriptor> track: <Artist> - <Title> <url>"

e.g. "Iwan's love track: Prince - Open Book https://youtu.be/...". Descriptor is free text; use an ordinal ("Iwan's second track: ...") to disambiguate. Multiple fallback tracks per description are separated by " / ". Names map: Mark→MW, Jack→JG, Jon/Jonny→JS, Iwan→IT. The S24 "Iron Man by Rahzel" case currently has NO url and must be rewritten to canonical format WITH a url.

Purpose: Make the fallback-track fix real in the live archive and confirm the UAT gap is closed.
Output: Corrected live database; confirmed spot-checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-archive-browsing/03-UAT.md
@.planning/phases/03-archive-browsing/03-01-SUMMARY.md
@.planning/phases/03-archive-browsing/03-04-PLAN.md
@app/sessions/[sessionNumber]/page.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Admin edits playlist descriptions to canonical fallback format</name>
  <what-built>Plan 03-04 added parseFallbackTracks + an import route that inserts each fallback track as its own row attributed to the named person. The parser reads title/artist/person/url ENTIRELY from the description text — nothing is fetched from YouTube — so the descriptions must be in the canonical format before re-import.</what-built>
  <how-to-verify>
    In Apple Music, edit the description of EVERY session that has a YouTube fallback track (known: sessions 3, 9, 15, 19, 31 — plus session 24's "Iron Man by Rahzel" case) so each fallback track reads:

      "<Name>'s <descriptor> track: <Artist> - <Title> <url>"

    Rules:
    1. Use the person's first name — Mark, Jack, Jon (or Jonny), Iwan. Jon and Jonny both map to JS.
    2. Separate MULTIPLE fallback tracks in one description with " / " (space-slash-space).
       - Session 3 has TWO (e.g. "Iwan's love track: <artist> - <title> <url> / Jonny's angry track: <artist> - <title> <url>").
       - Session 31 has Jonny's diamond track: "Jonny's diamond track: DND - Diamond Rings https://www.youtube.com/watch?v=dVXvm5HpCi8".
    3. Session 24: rewrite "Iron Man by Rahzel" into canonical "<Name>'s <descriptor> track: Rahzel - Iron Man <url>" and ADD a working YouTube url (it currently has none).
    4. Keep the existing initials attribution string (e.g. "MW, JG, JS, IT" / MIA markers) intact in the description — the parser still reads it for the Apple Music tracks.
    5. Use an ordinal descriptor ("Iwan's second track: ...") if one person has more than one fallback track in a session.

    Save each description in Apple Music. Do NOT re-import yet — that is Task 2.
  </how-to-verify>
  <resume-signal>Type "descriptions updated" once every fallback-session description is in canonical format (including S24 with a url), or list any session you were unable to update.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-import via MusicKit, then spot-check fallback tracks</name>
  <what-built>The import route now inserts each parsed fallback track as its own track (appleId null, youtubeUrl set) attributed to the named contributor and appended at the session's end. The session detail page already renders such a track correctly — it shows the Apple Music link ONLY when appleId is non-null and the YouTube link when youtubeUrl is set, with the contributor chip driven by attributedContributorId (no page code change needed).</what-built>
  <how-to-verify>
    1. Sign in as admin, open the dashboard, connect Apple Music (MusicKit authorize), and trigger "Sync sessions". Wait for the import to complete with no error toast.
    2. Open `/sessions/3`: confirm TWO fallback tracks appear, each with a YouTube (play) icon link and NO Apple Music link, each grouped under the correct contributor chip (e.g. Iwan/IT and Jonny→JS).
    3. Open `/sessions/31`: confirm Jonny's diamond track "DND - Diamond Rings" appears under the JS chip with a working YouTube link and NO Apple Music link. Confirm Iwan's position-1 Grace Jones track "I've Seen That Face Before" now shows its Apple Music link ONLY and NO YouTube link (the mislabelled link from the UAT report is gone).
    4. Spot-check `/sessions/24`: the former "Iron Man by Rahzel" fallback now appears as its own track under the correct contributor with a working YouTube link and no Apple link.
    5. Spot-check `/sessions/9`, `/15`, `/19`: each fallback track renders as its own entry with a YouTube link and correct contributor, and no YouTube link is attached to any Apple Music track.
    6. Optional DB spot-check if convenient: the tracks with youtube_url set should also have apple_id NULL (fallback rows are distinct from Apple rows).
  </how-to-verify>
  <resume-signal>Type "verified" if S3 shows 2 fallback tracks, S31 shows Jonny's diamond track under JS, and no fallback link is attached to the wrong track. Otherwise describe exactly what rendered wrong (session, track, link).</resume-signal>
</task>

</tasks>

<verification>
- Live re-import completes without error.
- S3 renders 2 fallback tracks (YouTube link, no Apple link, correct contributors).
- S31 renders Jonny's diamond track under JS (YouTube link, no Apple link); IT's position-1 track no longer carries a YouTube link.
- S24 fallback now exists as its own track with a url; S9/S15/S19 fallbacks render correctly.
- No YouTube link appears on any Apple Music track.
</verification>

<success_criteria>
- The UAT test-4 gap (BROWSE-03) is closed: every YouTube fallback track appears as its own track, attributed to the person who chose it, with a working YouTube link and no Apple Music link.
- The specific reported defect (S31 diamond track mislabelled onto Iwan's Apple track) is resolved.
</success_criteria>

<output>
Create `.planning/phases/03-archive-browsing/03-05-SUMMARY.md` when done, recording the re-import outcome (session/track counts, fallback-track count) and the spot-check results for S3, S24, and S31.
</output>
